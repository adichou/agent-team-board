# 测试用例 — BUG-20260910-009 任务模块搜索不过滤批量完善/批量开发面板

自动化测试：`scripts/tests/batch-search-filter-20260910-009.test.mjs`（node 直接运行；静态契约 + vm 全源码渲染断言，参照 tasks-panel-26 / search-module-20260910-009 风格）。
约束基线：`q` 为空（缺省）时所有列表输出与现状逐字一致（零回归）；过滤纯前端、不发任何请求；匹配大小写不敏感、包含即命中。

## F 接线与面板消费

- [ ] F1 两面板消费 `state.search?.q`（函数签名保持空参，兼容既有契约测试提取）；`renderBatchDrawer` 对 `renderCodexPanel(q)` 的既有传参保留；runSearch runs 分支仍走「清签名 + 重渲染」链路。
- [ ] F2 vm 全源码渲染：`state.search.q` 命中条目编号 / 标题（大小写不敏感）时，批量完善启动态候选队列与运行态待完善队列收窄到命中行；清空 `q` 恢复全量（同一次断言内对照）。
- [ ] F3 vm 全源码渲染：批量开发启动态待开发队列、运行态待处理队列按 `q` 收窄与恢复。
- [ ] F4 概况页签与启动区不随关键词收窄：候选统计数字、启动按钮资格（有无候选）按全量判定，与 `q` 无关。

## L 列表过滤（共用函数 / 排队批次）

- [ ] L1 `pendingQueueHtml(pending, { q })`：按 `id`/`title` 过滤；命中时「待处理队列（N 条被搜索过滤）」标注、「最近 X 条 / 共 Y 条」随命中收窄；全量命中为零且队列非空时显示「没有匹配的待处理条目，清空搜索恢复。」；队列为空仍显示「暂无待处理条目。」（两空态不混淆）；不传 `q` 输出与现状一致。
- [ ] L2 `runAttemptsHtml(records, total, kind, q)`：按 `runId`/`itemId`/`title`/`owner`（执行器）过滤；「（N 条被搜索过滤）」标注；「共 N 条」保持全量账面不收窄；无匹配显示「没有匹配的处理记录，清空搜索恢复。」；records 为空仍「暂无执行记录」；不传 `q` 输出与现状一致。
- [ ] L3 vm 渲染运行态开发面板：排队批次按 `batchId`/`developer`（开发人员）过滤收窄、无匹配显示「没有匹配的排队批次，清空搜索恢复。」；无排队批次时整节不显示（现状保留）。

## C 一致性与不回退

- [ ] C1 反馈条 runs 分支文案与真实过滤口径一致：含「前端过滤」「执行器」「批次号」与「不发请求」，不再宣称与面板行为不符。
- [ ] C2 `renderCodexPanel(q)` / `renderCxRuns(q)` 深链过滤源码零改动（回归护栏）。
- [ ] C3 过滤逻辑纯前端：两面板与列表渲染函数内不出现 `/api/` 请求调用。

## M 人工验收（README 验收说明 1–3，浏览器）

- [ ] M1 「批量完善 / 批量开发 × 启动态 / 运行态」四组合：输入编号片段 / 标题片段 / 执行器名 / 批次号（`RFB-`、`batch-`），对应列表收窄，清空（✕ / Esc / 删空）恢复全量。
- [ ] M2 一级 / 二级页签切换与 2 秒轮询期间关键词持续生效（不丢词、不闪回全量）。
- [ ] M3 无匹配空态与「暂无」空态不混淆；反馈条输入时出现、清空后隐藏。
