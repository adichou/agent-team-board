# 设计 — BUG-20260908-019 创建讨论正文留空，点击创建按钮提示正文不能为空。

> 由 Agent 在 /dev 开发前补充，人可随时批注。

## 引入来源（源单）

本 Bug 由哪个需求 / Bug 引入？登记时可暂空或写「未定位」，修复阶段必须归因（三选一，禁止编造）：

- **引入来源：未定位（排查过程：非代码缺陷，无引入源单。当前磁盘代码创建链路三处——前端 `submitNew()`（scripts/web/app.js，仅拦空标题，2369 行注释明确「不再拦截」）、服务端 `POST /api/oncall/ticket`（scripts/server.mjs:1035，直接转调 store 无额外校验）、store `createTicket`（scripts/lib/oncall-store.mjs:243，空白正文回退已 trim 的标题）——均已无任何「正文不能为空」拦截；vm 行为测试（oncall-question-optional U1~U3）、store 测试（oncall-store S6/S6b/S6c）、全新进程端到端 HTTP 测试（oncall-serve H4：只传 title / 空串 question → 200 且第 1 轮问题回退标题）全绿，全套 98 个测试文件 0 失败。判定为登记时环境滞后：REQ-20260908-013 改码 2026-09-08T13:42:37Z 已落盘，Bug 14:13:36Z 经看板登记时浏览器页面未刷新（旧 app.js 在内存）或 server.mjs 进程未重启（Node ESM 进程内缓存 lib 模块、无热重载）仍运行改码前旧版，两者均会弹出含「正文不能为空」的提示且弹窗保留；当前 8888 端口服务实测 2026-09-08T14:34:56Z 才启动，证明登记时刻的服务是另一更早进程。）**
- 佐证：创建时正文必填为 oncall 初版口径（REQ-20260907-001「新建咨询单：表单含标题、问题正文」，经 `atb list` 核验存在），REQ-20260908-013 已将其改为可选（其 README 记录了改码前三处拦截的原文位置）；本 Bug 登记时该拦截在磁盘代码中已不存在，故无引入源单可归。

## 根因分析

**结论：环境滞后，非代码缺陷。** 登记人在 REQ-20260908-013 完成上报（13:42:37Z）约 31 分钟后（14:13:36Z）经看板登记本 Bug，当时其运行环境仍执行改码前版本：

1. **磁盘代码无拦截（已逐处核验）**：
   - 前端 `submitNew()`（scripts/web/app.js:2358）：仅校验标题（2366 行「标题不能为空」），2369 行注释「REQ-20260908-013：讨论单正文可留空……不再拦截」；全仓 grep「正文不能为空」仅剩追问（oncall-store.mjs:357、web/oncall.js:468）、回答（oncall-store.mjs:449）与测试断言，均为设计保留口径。
   - 服务端 `POST /api/oncall/ticket`（scripts/server.mjs:1035）：直接转调 `oncall.createTicket`，无独立校验。
   - store `createTicket`（scripts/lib/oncall-store.mjs:243-251）：空白 `question` 回退已 trim 的标题，`question.md` 恒有内容。
   - web 静态资源仅 `scripts/web/` 一份（无构建产物/副本），不存在第二处旧拦截源。
2. **测试复测最新环境不复现（全绿）**：oncall-question-optional（U1 静态断言 app.js 不再含拦截文案、U2 留空直接发请求、U3 填写正文回归）、oncall-store S6/S6b/S6c（缺省/空串/纯空白回退标题、非空原样落盘、留空+附件）、oncall-serve H4（**全新启动服务进程**，POST 只传 title / 空串 question → 200 + 详情第 1 轮问题为标题，即 README 验收第 1 条「最新代码环境」复测）；`npm test`（run-all）98 个测试文件 0 失败。
3. **进程时间线证据**：`GET /api/health` 实测当前 8888 端口服务（pid 70456）`startedAt: 2026-09-08T14:34:56.796Z`，晚于 Bug 登记（14:13:36.997Z）——登记时刻在 8888 端口服务的是另一更早启动的进程，与「候选二：服务进程滞后」（改码前启动、进程内旧版 `oncall-store.mjs` 抛「问题正文不能为空」，前端 toast 显示「创建失败：问题正文不能为空」）吻合；「候选一：浏览器页面滞后」（旧 app.js 内存版 `submitNew` 直接 toast「讨论单的问题正文不能为空」且不发请求）同样成立，两者均与登记原文「文案含『正文不能为空』、弹窗保留」相容。
4. **候选三（入口误记）**：登记原文明确「顶栏『＋ 新建』→ 类型选讨论（ASK）→ 点『创建』」，与创建弹窗路径一致；追问入口提示文案为「追问内容不能为空」（不含「正文」），与登记文案「正文不能为空」不符，可能性低（登记人入口确认仍待人工，若确认误记则同理按非缺陷关闭）。
5. 时间线相容性成立：改码落盘（≤13:42Z）→ 登记（14:13Z）→ 服务重启（14:34:56Z，此后进程内已含回退逻辑；oncall-store.mjs 磁盘最后修改 14:20:26Z 早于该次启动，加载的即含回退版本；app.js 为静态文件 `Cache-Control: no-store` 直读磁盘，页面刷新即最新）。

## 方案

**不改代码**（无代码缺陷可修）。按 README 验收路径「环境滞后」收尾：

1. 复测确认最新代码行为正确（上述测试全绿，含全新进程端到端 H4），无需新增用例——REQ-20260908-013 已沉淀完整回归网（静态断言防拦截文案回流、vm 行为、store 回退、端到端），补重复用例无增量价值。
2. 本 design.md「引入来源（源单）」节写明结论与排查过程；README 头部补 `- 引入来源：` 行（第一屏可见）。
3. 用户侧操作指引（登记人复验前执行）：刷新看板页面；如仍复现则重启 `server.mjs` 进程。当前 8888 服务已于 14:34:56Z 重启（加载含回退逻辑的 oncall-store），刷新页面即可直接复验。

## 风险与边界

- 当前 8888 服务（pid 70456，14:34:56Z 启动）进程内已含正文回退逻辑，线上不受影响；后续再修改 `scripts/lib/` 下模块仍需重启服务才生效（Node ESM 无热重载，已知特性，不另行立项）。
- 追问（「追问内容不能为空」）与回答（「回答内容不能为空」）必填为 REQ-20260908-013 明确保留的设计口径，不在本 Bug 范围。
- 候选三的登记人入口确认属人工环节：若日后确认当时实为追问入口，本 Bug 结论不变（同为非代码缺陷）。
- 本机尚有多个历史遗留 server.mjs 进程（多为测试残留，端口各异），不影响 8888 端口当前服务；不代为清理用户环境进程。
