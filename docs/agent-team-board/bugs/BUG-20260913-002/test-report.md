# 测试报告 — BUG-20260913-002 新建版本和版本查看界面的需求或 bug 如果标题过长，需要显示 tips

- 时间：2026-09-13T11:31:50.757Z
- 执行者：atb-0913-e0be
- 测试框架：node:assert/strict + 前端 vm 行为测试（scripts/tests/run-all.mjs 聚合）
- 覆盖率：100%

## 总结

人工优化口径落地：候选行 tips 只显示完整标题、不含单号（与详情正文条目行既有形态一致），行内仍展示「单号 + 标题」。实现为 renderCandidateRows() 标题 span 补 title="${esc(it.title)}"，esc 转义防属性截断/注入，一处修复覆盖新建版本与添加条目两面板，CSS/候选口径/勾选联动零改动。新增 4 用例 TDD 红→绿（两面板 tips 仅标题、特殊字符转义、行内文案不变），构建模块既有回归 4 文件 25 用例通过，全仓 212 测试文件全量回归失败 0。

## 明细

（可粘贴命令输出、失败用例说明等）
