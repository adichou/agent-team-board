# 回归用例

1. 首次打开：消息节点尚不存在，仍发出详情请求；响应后加载文字隐藏、表单显示。
2. 请求失败：加载文字隐藏、错误信息显示；重试成功后切换为表单。
3. 关闭重开：清除旧消息，重新加载并显示表单。
4. 关闭后响应迟到：不重开面板，不写入详情缓存。

测试文件：scripts/tests/bug-confirm-panel-open-20260915-001.test.mjs。
