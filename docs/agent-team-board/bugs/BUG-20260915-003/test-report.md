# 测试报告 — BUG-20260915-003 提交确认面板计数与核验不一致、缺少待提交差异且 Git 错误被截断

- 时间：2026-09-15T03:16:40.926Z
- 执行者：zcode-batch-048-034
- 测试框架：Node assert + 真实临时 Git 端到端 + vm 渲染 + server HTTP（run-all 自研 harness）
- 覆盖率：9%

## 总结

统一挂起确认计数/文件表/核验/补交为同一候选范围口径（本单可归属/归属待确认），git add 失败空 pendingManual 也展示候选或待核对不显示 0；全局文件归属待确认须显式计入/排除、默认不静默并入；自动提交失败保留完整错误 errorFull（0 组也落明细）并在面板摘要+全文+复制展示；差异读取失败明确报错可重试；指纹覆盖全候选、重新核验刷新基线拦截内容变化。引入来源 REQ-20260914-001。新增 9 用例先红后绿，全量 242 测试文件 0 失败

## 明细

（可粘贴命令输出、失败用例说明等）
