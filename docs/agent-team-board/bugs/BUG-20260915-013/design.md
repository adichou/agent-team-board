# 修复设计 — BUG-20260915-013

## 引入来源与根因

引入来源：REQ-20260910-029，已通过 `atb show` 核验。Git 历史最早可见聚合提交 `6f2ead6`，模块与测试头部标注该需求。发布流水线的 `gitCmd` 整体 trim stdout 导致首行 ` M ` 状态列丢失一个空格，后续 `slice(3)` 截断路径首字母。

## 实施

为 `gitCmd` 增加默认关闭的 `raw` 选项，仅 local-precheck 的 porcelain 读取启用。保留其他命令现有 trim 行为、错误处理与日志；目录豁免、重命名和引号处理原样保留。

开源选型：直接复用既有 Node/Git 执行器，仅需保留已有 stdout 原文。引入库的成本高于这一局部修正，无新增依赖或复制代码。

## 验证与边界

真实隔离仓库新增单条/多条看板修改、首行普通修改、混合修改、暂存、未跟踪与空输出测试；逐例核对命令、远端、HEAD、暂存区与文件内容。补充 merge/rebase 阻塞回归。产品发布模块沿用原实现并运行既有回归。

预检通过只代表 local-precheck 完成，未将其解释为发布成功；完整发布由 G1/G4–G10 使用临时本地 bare 远端验证。未修改目录豁免与复杂路径规则，未改动用户已有文件或提交工作区。
