# 测试用例 — REQ-20260908-026 优化批量任务管理界面与 Agent 配置

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。
> 数据层用例落在 `scripts/tests/tasks-retry.test.mjs`，界面契约用例落在 `scripts/tests/tasks-panel-26.test.mjs`；
> 存量受影响断言（refine-ui R12-11、tasks-refine 空列表校验、refine-serve 400、planned-state / next-batch-entry / batch-ui 文案）随口径更新。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| 1 | 两类任务启动按钮文案均为「启动」，不再出现「启动批量完善 / 启动批量开发」 | 高 | ✅ |
| 2 | 创建 + 复制成功提示含「任务已创建、提示词已复制，请在对应项目会话粘贴发送」；复制失败提示保留任务并提供重新复制/手动复制 | 高 | ✅ |
| 3 | 运行面板计数为「已处理 / 异常 / 处理中 / 待处理」；当前项显示耗时 | 高 | ✅ |
| 4 | 无当前项按阶段展示：待启动 / 等待领取 / 已暂停 / 已结束；终态不显示「等待领取」 | 高 | ✅ |
| 5 | 待处理队列只显示最近 2 条（最新在前），标注「最近 X 条 / 共 N 条」；0/1/2/>2 均正确；>2 时提示仅展示限制 | 高 | ✅ |
| 6 | 本轮处理记录为四列表格（需求 / Bug、执行状态、次数、操作），只显示最近 2 次尝试并标注「最近 X 条 / 共 N 条」；不出现「加载更多」 | 高 | ✅ |
| 7 | 记录能区分需求 / Bug（前缀判定），显示状态、第 N 次尝试与异常原因；异常/已中断行提供「重新执行」，其余为「—」 | 高 | ✅ |
| 8 | 暂停按钮文案「暂停后续领取」；开发面板终态不再显示暂停/恢复控件（对齐完善面板既有口径） | 高 | ✅ |
| 9 | 任务设置为四列表格（执行 Agent / 子代理模型 / 推理强度 / 是否隐藏），按批量完善、批量开发两块配置，固定 Codex、Zcode 两行；默认不勾选隐藏 | 高 | ✅ |
| 10 | 设置保存按「隐藏」反算可见 Agent；全部隐藏允许保存，启动区禁用并提示到任务设置取消隐藏；已有配置迁移保留 | 高 | ✅ |
| 11 | 数据层（批量开发）：retryRun 对 failed/blocked/interrupted 记录重排队（进入本轮待处理、计数改记待处理、保留原记录）；重复点击幂等不产生重复在途 | 高 | ✅ |
| 12 | 数据层（批量开发）：retryRun 核验占用——条目被认领（in-progress 且有 owner）拒绝并给出人工处理指引；有在途执行拒绝；非异常终态（已上报）拒绝 | 高 | ✅ |
| 13 | 数据层（批量开发）：重排队后 next 重新领取该条目并创建新运行（尝试次数 +1 的账面基础），retryItems 标记清除 | 高 | ✅ |
| 14 | 数据层（批量完善）：retryRefineRun 同口径——failed/interrupted 记录重排队尾、保留原记录、计数改记待处理；条目非 accepted 或有在途执行拒绝 | 高 | ✅ |
| 15 | 记录带尝试次数字段（attempt = 本任务内该条目的运行数），/api/{batch,refine}/current 提供 recordsTotal 与待处理队列（pending） | 中 | ✅ |
| 16 | 服务端新增 POST /api/batch/retry、/api/refine/retry（终态任务重试由前端复用 create + 复制提示词承接） | 中 | ✅ |
| 17 | 任务模块头部提供「任务设置」入口跳设置模块；生产界面无 Demo 场景按钮 | 中 | ✅ |
| 18 | 回归：atb refine / atb batch CLI 既有命令与 claim/report 状态机、impl 互斥不受影响（存量测试全绿） | 高 | ✅ |
