# 测试用例 — REQ-20260830-005 看板卡片一键复制 REQ/BUG 单号

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。
> C1–C4 由 `scripts/tests/copy-id.test.mjs`（静态契约）覆盖；C5 为浏览器实测。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| C1 | app.js 存在 copyId 逻辑：优先 navigator.clipboard.writeText，含 execCommand 降级与「手动框选」失败提示 | P0 | ✓ |
| C2 | 卡片单号（cardEl）与抽屉单号（renderDrawer）均绑定复制（stopPropagation 防冒泡开抽屉），单号元素带可点标识 | P0 | ✓ |
| C3 | 反馈就地显示「已复制」（约 1.2s 后恢复），不使用全局 toast 作为成功反馈 | P0 | ✓ |
| C4 | style.css 含 .cid 可点态（cursor/hover）与「已复制」反馈样式，明暗主题均用全局变量 | P1 | ✓ |
| C5 | 浏览器实测：点击卡片单号 → 剪贴板为纯单号 → 单号处显示「已复制 ✓」后恢复；点击单号不打开抽屉；拖拽与轮询不受影响 | P0 | ◐ |

## 执行记录（2026-08-30）

- C1–C4：`node scripts/tests/copy-id.test.mjs` 全绿（先红后绿：初跑 4 项全失败）。
- C5（部分通过）：服务载入新前端后实测，但当日 IAB 输入管线再次故障（点击卡片任意位置均无事件响应、截图报 guest 错误），无法完成点击目验。复制逻辑（Clipboard API → execCommand 降级 → 手动框选提示）已由 C1 契约覆盖；事件绑定与防冒泡由 C2 覆盖；127.0.0.1 为安全上下文，Clipboard API 可用性有保障。待人工点击任一单号即可确认（复制后单号变「已复制 ✓」且不打开抽屉）。
- 覆盖位置：看板卡片、抽屉详情头、抽屉下属 Bug 列表三处单号均可复制。
