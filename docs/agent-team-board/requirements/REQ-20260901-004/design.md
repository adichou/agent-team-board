# 设计 — REQ-20260901-004 看板按钮文案简化：「对齐确认」改为「对齐」

> 由 Agent 在 /dev 开发前补充，人可随时批注。**状态：待对齐（方案初稿，未经人工确认不得实施）。**

## 背景

待对齐详情的操作按钮当前文案为「▶ 对齐确认」，确认弹窗文案「对齐确认（开始实施）」。用户反馈保留「对齐」即可——动作本身已含确认语义。

## 方案（初稿，待对齐）

纯文案改动，`scripts/web/app.js` 三处 + 一处测试：

1. `drawerActionsHtml` 待对齐分支：按钮文案 `▶ 对齐确认` → `▶ 对齐`；`data-label` 由「对齐确认（开始实施）」改为「对齐（开始实施）」。
2. 前置 notice 文案「确认可行后点『对齐确认』进入实施」→「确认可行后点『对齐』进入实施」。
3. `pending-alignment.test.mjs` P6 的断言「对齐确认」→ 同步改为断言 `▶ 对齐`（含 data-label 文案）；其余五套测试不涉及该文案。
4. SKILL.md / dev.md 中的「对齐确认」是**流程动作描述**（区别于按钮文案），保留不动——若你希望文档也统一叫「对齐」，对齐时说明，一并改。

## 影响面

`app.js`（3 处字符串）、`pending-alignment.test.mjs`（1 条断言）。样式、状态机、server 零改动。

## 风险与边界

无行为变化；按钮语义（pending-alignment → in-progress，人工专属）不变。
