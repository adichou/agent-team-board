# 测试用例 — REQ-20260901-004 看板按钮文案简化：「对齐确认」改为「对齐」

> 方案见 design.md（**待对齐**）。对齐确认后执行。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| B1 | app.js 待对齐按钮文案为「▶ 对齐」，data-label 为「对齐（开始实施）」，notice 同步简化 | P0 | ✓ |
| B2 | 点击行为不变：data-act 仍为 in-progress（待对齐 → 开发中，人工专属） | P0 | ✓ |
| B3 | pending-alignment.test.mjs P6 断言收紧为按钮文案（>▶ 对齐<，且断绝 >▶ 对齐确认<）；六套测试回归全绿 | P0 | ✓ |


## 执行记录（2026-09-01）

- 实施方式：B1 的三处文案改动随 REQ-20260901-001 三次修订一并落地（同一批字符串）；本条目对齐确认后补齐
  B2 核验（data-act 行为未变）与 B3（P6 断言收紧，按钮文案精确断言）。
- `node scripts/tests/pending-alignment.test.mjs` P1–P8 全绿（P6 收紧后）。
