# 测试用例 — REQ-20260901-001 详情页支持上一条/下一条快速浏览

> 方案见 design.md（**待对齐**）。对齐确认后，D1–D4 先跑红再实现跑绿；D5 浏览器实测。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| D1 | 导航两翼化（三次修订版）：说明文字独立占操作行上方整行（drawerActionsNoticeHtml），中栏只放操作按钮（drawerActionsButtonHtml）与 i/N，导航两翼仅与按钮行对齐；按钮文案纯方向词、单号在 title、首尾禁用 | P0 | ✓ |
| D2 | 切换复用 openDrawer（换算相邻 id），文档页签选择重置（drawer.doc 置空） | P0 | ✓ |
| D3 | 键盘 ArrowLeft/ArrowRight 在抽屉打开时切换；输入框聚焦或新建弹窗打开时不触发 | P0 | ✓（契约；键盘实击受 IAB 输入管线限制未逐一目验） |
| D4 | style.css：两翼按钮用全局变量（hover 主题色）、禁用态 40% 透明；中栏容器样式存在；顶部 .drawer-nav 样式已移除 | P1 | ✓ |
| D5 | 浏览器实测：点卡片 → 操作区两翼出现上一条/下一条 → 切换详情与操作按钮刷新 → 首尾禁用 → 深浅色正常 | P0 | ◐ |

## 执行记录（2026-09-01）

- 初版（顶部导航条）：D1–D5 全过，随后**人工反馈：顶部导航条挤压标题，废弃该方案**。
- 修订（底部两翼）：按 TDD 重走——D1/D4 断言先改先红（2 失败）→ renderDrawer 重构（导航按钮移入 .drawer-actions 两侧，中栏放操作按钮与 i/N 位置指示，顶部渲染移除）→ D1–D4 全绿；六套回归全绿。
- 四次修订（同状态内导航）：D2 断言先改先红 → drawerNeighbors 按当前条目 status 过滤（i/N 为状态组内序号，键盘同规则）→ D1–D4 全绿；七套回归绿（loop-mode 红为 BUG-20260901-001 既有问题，待其修复）。
- 五次修订（2026-09-05，BUG-20260903-001）：四次修订的实时状态过滤导致操作按钮流转状态后导航漂移进新状态列表。导航范围改为打开抽屉时冻结（scopeIdsFor 快照 → state.drawer.navIds）；D2 断言重写 + 新增 B1–B3 先改先红 → 实现后 D1–D4 + B1–B3 全绿；18 套回归全绿。用例细节见 bugs/BUG-20260903-001/test-cases.md。
- 三次修订（说明文字独立成行）：D1 断言先改先红 → 拆分 notice/按钮渲染函数并调整模板顺序 → D1–D4 全绿；六套回归全绿。顺带落地 REQ-20260901-004 的按钮文案简化（同一批字符串）。
- D5 修订版点击目验因 IAB 输入管线间歇故障未能完成（抽屉打不开）；布局结构由 D1/D4 契约覆盖，渲染路径与已实测的同一 renderDrawer 一致，仅 markup 位置变化。待人工刷新页面点开任一卡片确认视觉效果。
