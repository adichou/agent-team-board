# 测试报告 — REQ-20260906-018 批量实施入口改造：已接受列多选后进入，移除顶栏按钮

- 时间：2026-09-06T11:36:47.434Z
- 执行者：zcode-batch-entry
- 测试框架：node:assert + node:vm 行为测试 / NODE_V8_COVERAGE 新增函数体覆盖 100% / 内置浏览器实测
- 覆盖率：100%

## 总结

按人工反馈修订：已接受列批量实施改为与批量接受一致的常驻交互（无模式）——工具栏常驻「全选/已选 N/进入批量实施」与待接受列 accept-toolbar 同构，已接受卡片复选框常驻；0 勾选进入抽屉=默认范围（按钮不再禁用，保证批次创建/账本查看可达），有勾选范围=勾选集合；Esc 链还原（批量抽屉→弹窗/详情），删除入口按钮/取消/模式机件。范围机制（scope 推送、ids 建批、失效剪枝 toast、两页签范围行）不变。附带修复：batchSig 未含 stats 导致范围切换不重渲染残留旧范围行——计入签名并新增 E11。E1–E11 先红后绿，全量 37 文件仅存量 BUG-20260906-011(T2) 失败；浏览器复测通过。另：人工反馈的「待接受卡片接受按钮移至单号右侧」已登记 REQ-20260906-020（submitted，017 交付物范围，未动）。

## 明细

（可粘贴命令输出、失败用例说明等）
