# 测试用例 — REQ-20260906-017 支持对需求或 bug 单批量接受

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| A1 | 待接受列操作栏、空选择禁用、全选与部分选中态 | P0 | 通过 |
| A2 | 需求、独立 Bug、下属 Bug 卡片选择/接受不冒泡；其他状态无控件 | P0 | 通过 |
| A3 | 单条接受与混合批量接受调用原接口，项目参数和目标状态正确 | P0 | 通过 |
| A4 | 空选择、取消确认不请求；重复单号只提交一次 | P0 | 通过（与 A3 合并执行） |
| A5 | 请求期间禁用操作，重复点击不重复提交 | P0 | 通过 |
| A6 | 部分失败继续后续项、汇总结果、保留失败选择并可重试 | P0 | 通过 |
| A7 | 全部失败/网络异常反馈，恢复控件且无假成功 | P0 | 通过 |
| A8 | 轮询保持有效选择，状态变化/删除剔除，新条目不自动选择 | P0 | 通过 |
| A9 | 切换项目清空选择和反馈；原项目在途请求不串写，不发后续旧请求 | P0 | 通过 |
| A10 | 旧项目轮询响应不覆盖新项目 | P0 | 通过 |
| A11 | 卡片/详情/拖拽单条入口兼容，不改变非接受操作 | P1 | 通过 |
| A12 | 现有状态 API 的人工流转、非法流转和项目隔离回归 | P0 | 通过（pending-alignment / multi-project 等现有集成测试） |
| A13 | 勾选样式、卡片和详情接受按钮禁用及恢复 | P0 | 通过 |
| M1 | 桌面与窄屏、深浅主题、键盘选择、详情与复制互不冲突 | P1 | 控件可见性已核对；点击自动化未获授权，其余待人工 |

## 执行记录

- 红阶段：node scripts/tests/accept-ui.test.mjs，10 组失败，证据 evidence/red.log。
- 绿阶段：同命令，最终 11 组通过（新增 A13），证据 evidence/green.log。
- 完整回归：npm test，34 个文件，33 通过、1 个已知失败 BUG-20260906-011，证据 evidence/full-test.log。
- 静态语法：node --check scripts/web/app.js，通过。
- 未把任何真实条目改为 accepted 或 done；模拟网络接受及临时项目 HTTP 集成测试覆盖提交行为。

## 人工验收步骤

1. 打开本项目看板，在「待接受」列用鼠标/键盘选择一条需求和一条 Bug，核对已选数量、全选的部分选中态及不打开详情。
2. 点击「接受所选」并取消，核对状态不变；确认接受经人工挑选的测试条目，核对只移动所选项且计数刷新。
3. 在另一窗口改变一个所选测试条目的状态，核对失败反馈或轮询移除选择；检查剩余条目可重试。
4. 切换项目，核对选择及结果清空；分别在桌面、窄屏、深浅色下检查操作栏、卡片和结果区。
5. 检查报告后，在本条目点击「确认完成」。
