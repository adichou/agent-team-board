# 测试用例 — REQ-20260909-004 开放式讨论模块重构与纪要生成需求或 Bug

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| S1 ✓ | 创建讨论：标题必填 ≤120、背景可选、状态默认讨论中、无 reqId；编号为 ASK 序列 | P0 | 通过 |
| S2 ✓ | 两态映射：旧执行状态（pending/answering/answered/failed）读时归一为讨论中；archived 为已归档，旧记录不被改写 | P0 | 通过 |
| S3 ✓ | 归档 / 继续讨论：随时可归档无须成果；往返切换不清空纪要、草稿与已创建条目记录 | P0 | 通过 |
| S4 ✓ | 提示词：启动提示词含编号/项目根/背景/落盘约定三件套路径；收尾提示词幂等（重复点击只记一次 finishPromptAt），状态保持讨论中 | P0 | 通过 |
| S5 ✓ | 成果读取：无 PUBLISH.json → waiting；标记编号不符 / 缺纪要 / candidates 非法（绑定不符、缺 items、类型非法、标题空或超长、重复 id）→ error 带可读原因；成套 → published 并回写 publishedAt（幂等） | P0 | 通过 |
| S6 ✓ | 草稿创建：勾选项走 createItem 初始 submitted；README 含来源讨论行与验收/复现内容；status.json 带 sourceDiscussion；ticket.created 记录草稿映射 | P0 | 通过 |
| S7 ✓ | 创建幂等：已创建草稿再次提交直接跳过不重复建条目；标题为空 / 超长的草稿逐项失败并可仅重试失败项（其他条目不受影响） | P0 | 通过 |
| S8 ✓ | 零候选：candidates.items 为空数组合法（纪要可只有结论）；详情成果计数正确 | P1 | 通过 |
| H1 ✓ | GET /api/discussion/board 返回两态卡片（含计数与阶段提示）；未初始化项目返回 initialized:false 空态 | P0 | 通过 |
| H2 ✓ | POST /api/discussion 创建成功返回含启动提示词的全量；标题缺失 400 | P0 | 通过 |
| H3 ✓ | finish / archive / resume / reread 动作接口返回更新后的全量 | P0 | 通过 |
| H4 ✓ | POST create-items 返回逐项 results（成功含 itemId、失败含 error）且不重复创建成功项 | P0 | 通过 |
| U1 ✓ | 骨架：两档胶囊筛选（讨论中/已归档）+ 实时计数；宽屏左右分栏复用 req-split；窄屏详情覆盖 + 返回列表保留筛选与滚动位置 | P0 | 通过 |
| U2 ✓ | 列表行展示标题、编号、两态状态、最近更新时间与成果提示副标题；仅两档筛选无第三态 | P0 | 通过 |
| U3 ✓ | 详情头操作行：启动提示词 / 讨论完毕 / 归档↔继续讨论；提示词只读展示 + 复制按钮 + 剪贴板不可用手动复制回退 | P0 | 通过 |
| U4 ✓ | 阶段反馈：等待纪要 / 纪要已生成 / 有待创建草稿仅为提示不参与筛选；读取失败展示原因与重试入口，不渲染半成品 | P0 | 通过 |
| U5 ✓ | 草稿区：勾选默认开、类型只读、字段可编辑；必填校验逐项提示；未勾选项提示；创建中禁用；部分失败仅重试失败项；已创建项展示真实编号与跳转且不可重复创建 | P0 | 通过 |
| U6 ✓ | 新建：统一弹窗「讨论」类型仅标题（必填）+ 背景（可选），无关联需求与附件字段；保存后进入讨论模块定位新讨论并展示启动提示词 | P0 | 通过 |
| U7 ✓ | 双向关联：条目详情展示「来源讨论」跳转；讨论详情已创建成果可跳条目 | P1 | 通过 |
| U8 ✓ | 兼容：旧讨论单（含旧状态与旧轮问答）在新模块按讨论中展示且历史问答只读保留；既有 REQ/BUG 创建与状态规则不回归（全量测试） | P0 | 通过 |