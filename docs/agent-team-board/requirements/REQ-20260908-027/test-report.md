# 测试报告 — REQ-20260908-027 选择功能混乱，需要重构

- 时间：2026-09-08T17:41:27.398Z
- 执行者：zcode-batch-018-1
- 测试框架：node:test（vm 沙箱 UI 测试）
- 覆盖率：12%

## 总结

选择按档隔离重构：工具条只显示/统计/作用于当前筛选档（待接受→接受所选；已接受→移入计划+新增驳回待接受；已计划→进入批量开发+移出计划；其余档隐藏）；计数改『已选 N 项（当前档）』；结果区按档呈现。新增 rejectToSubmitted 批量驳回（共用已接受勾选，二次确认、逐条 accepted→submitted、单项失败不回滚、完善中由服务端拦截计入失败清单、切项目重置）。全选/全不选成对入口仅作用当前档（叠搜索，其他档勾选不动，跨档保留契约保持）；清空选择同口径收敛当前档；删除 .req-row.selected 蓝边框与 selected 类切换；批量完善按钮移出工具条（入口收敛任务模块+完善徽标）。新增 selection-lane-scope 12 用例先红后绿；更新 accept-ui/impl-entry-ui/plan-batch-move/refine-ui 既有断言到新口径；npm test 100 文件 0 失败；服务端零改动。

## 明细

（可粘贴命令输出、失败用例说明等）
