# 测试用例 — REQ-20260908-027 选择功能混乱，需要重构

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| S1 | 静态契约：工具条含 #planReject（驳回待接受）；列表标题行含成对「全选 #selectOperable / 全不选 #selectNone」；#refineGo 移出选择工具条（批量完善与勾选无关，入口收敛任务模块与完善徽标） | P0 | ✅ |
| S2 | 工具条按档收窄：待接受档只显示「接受所选」；已接受档只显示「移入计划」「驳回待接受」；已计划档只显示「进入批量开发」「移出计划」；开发中/待测试/已完成档工具条整体隐藏 | P0 | ✅ |
| S3 | 计数按档：#selCount 只统计当前档勾选（`已选 N 项（<档名>）`），不再出现三类合并计数；其他档残留勾选不计入；当前档勾选 0 时工具条隐藏 | P0 | ✅ |
| S4 | 批量驳回待接受：一次二次确认；逐条 POST `{to:"submitted"}` 且绑定当前项目；成功单移出勾选、失败单保留；完成反馈成功/失败计数；结果区分列失败原因 | P0 | ✅ |
| S5 | 批量驳回完善中拦截：refining 单流转被服务端拒绝（错误文案含「完善中」），进失败清单不回滚其余成功单 | P0 | ✅ |
| S6 | 全选：仅勾选当前筛选档内可见可操作条目（叠搜索范围）；当前档集合替换为所见集合，其他档勾选不动（BUG-20260907-016 契约保持）；当前档无可操作条目时禁用 | P0 | ✅ |
| S7 | 全不选：仅清当前档勾选（其他档保留）；已计划档全不选后同步推送空范围；当前档勾选 0 时禁用 | P0 | ✅ |
| S8 | 清空选择：工具条「清空选择」仅清当前档（口径与全不选一致） | P1 | ✅ |
| S9 | 视觉契约：style.css 不再有 `.req-row.selected` 主题蓝边框规则；app.js 不再对勾选行 toggle `selected` 类（全选/手动勾选均只有复选框打勾态） | P0 | ✅ |
| S10 | 进行中防误触：任一批量操作 pending 时全选/全不选禁用；已接受复选框在移入计划或驳回进行中均禁用；驳回按钮 pending 文案「驳回中…」 | P1 | ✅ |
| S11 | 结果区按档：批量反馈只在对应档显示（已接受档显示移入/驳回结果，切到待接受档不显示）；跨档勾选保留（切档不清空）不回退 | P1 | ✅ |
| S12 | 切换项目重置驳回态（pending/message/failures），驳回进行中切项目停止后续请求 | P1 | ✅ |
| R1 | 既有行为不回退：批量接受 / 批量移入计划 / 批量移出计划 / 进入批量开发在各自档内交互与计数语义保持（accept-ui、plan-batch-move、impl-entry-ui 既有用例随新口径更新后全绿） | P0 | ✅ |
