# REQ-20260906-012 单条派发任务中，会话名更改为单号 标题

- 状态：in-progress（zcode-dispatch-session-title 实施完成，待人工确认）
- 创建：2026-09-06T03:48:20.919Z

## 描述

Status Board「一键派发」生成的提示词（`scripts/web/app.js` 的 `dispatchPrompt`）目前让被派发 Agent
把会话名改为**单号**（REQ-20260904-001 引入）：

- zcode 版第二行：`请将当前会话名改为 <单号>。`
- codex 版句首：`请将当前会话名改为 <单号>，然后加载 agent-team-board skill（atb）…`

会话列表里只有单号，多会话并行时仍要逐个点开才知道各自在做什么。本次把两版改名指令的目标从
「单号」升级为「**单号 标题**」（如 `REQ-20260906-012 单条派发任务中，会话名更改为单号 标题`），
不点开即可辨识任务内容；该格式与看板抽屉导航悬停提示（`<单号> <标题>`）一致。

界面与交互不变：仅派发提示词文本变化。抽屉「一键派发」按钮、复制/拉起流程、深链兜底均保持原样；
终端标签标题（`scripts/lib/dispatch.mjs` 的 .command OSC 0 行）仍为单号——`ITEM_ID_RE` 兼作
.command 文件名与标签标题白名单，标题属自由文本，不引入终端转义序列（本条不改 `dispatch.mjs`）。

## 验收标准

- [ ] zcode 版提示词第二行变为「请将当前会话名改为 <单号> <标题>。」（单号与标题均模板变量注入，不硬编码）
- [ ] codex 版句首变为「请将当前会话名改为 <单号> <标题>，然后加载 agent-team-board skill（atb）…」
- [ ] 其余派发语义不变：zcode 版首行仍是 `/dev <单号>   # <标题>…`；codex 版仍是 skill 指引措辞
- [ ] `scripts/tests/dispatch.test.mjs` P1.5 契约同步更新（两版各一次、同时含单号与标题模板变量），该文件全绿
- [ ] `scripts/lib/dispatch.mjs` 无改动（终端标签标题仍单号，深链构造不变）
