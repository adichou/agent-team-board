# 测试用例 — REQ-20260906-012 单条派发任务中，会话名更改为单号 标题

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| T1 | P1.5 更新：`dispatchPrompt` 内「请将当前会话名改为 ${it.id} ${it.title}」恰好出现 2 次（zcode/codex 各一，单号与标题均为模板变量）——改 app.js 前跑红 | P1 | ✅ 跑红后跑绿 |
| T2 | 旧格式回归：不再存在「请将当前会话名改为 ${it.id}，」/「…${it.id}。」的仅单号形态（防止改漏一版） | P1 | ✅（P1.5 内嵌断言，旧形态匹配数 0） |
| T3 | P1 回归：模板函数仍区分 zcode/codex、含 `it.id`/`it.title`、zcode 版首行 `/dev ` 入口 | P1 | ✅ |
| T4 | P2/P3/P4 回归：派发按钮渲染、剪贴板通道、样式均不变 | P2 | ✅ |
| T5 | `dispatch-launch.test.mjs` 全绿（U/C/I 套件），`scripts/lib/dispatch.mjs` 无 diff（终端标签标题仍单号、深链不变） | P1 | ✅ |

说明：T1 即 `scripts/tests/dispatch.test.mjs` 的 P1.5 用例改造（REQ-20260904-001 → REQ-20260906-012），
T2 以 T1 的正则收紧天然覆盖（旧形态匹配数应为 0），不单独加用例；T3/T4 复用既有 P1/P2/P3/P4。
