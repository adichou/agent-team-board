# 测试用例 — REQ-20260906-016 创建完需求和 bug 单后，要自动导航到对应单

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

自动化（`scripts/tests/new-item-nav.test.mjs`，加载实际 app.js 的行为测试 + 接线静态契约）：

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| N1 | 基线播种：首轮 poll（knownIds 为 null）只登记全部 id，不导航、不开抽屉 | P0 | 通过 |
| N2 | CLI 新建检测：基线建立后 poll 出现新 REQ → openDrawer 跳到该单，state.drawer.id 更新 | P0 | 通过 |
| N3 | 一次出现多条新单：导航到 createdAt 最新者，其余 id 全部登记（下轮不再视为新） | P1 | 通过 |
| N4 | 无新单：poll 数据变化（状态流转/更新时间）不触发导航，已有抽屉仅常规刷新 | P0 | 通过 |
| N5 | 弹窗路径：submitNew 创建成功后关闭弹窗并 openDrawer(新 id)；失败时弹窗保留、报 toast、不导航 | P0 | 通过 |
| N6 | 护栏：新建弹窗或批量实施抽屉打开时检测到新单不导航，但 id 已登记（关闭后不补跳） | P1 | 通过 |
| N7 | 切换项目：knownIds 重置为新项目首轮播种，不把新项目既有条目当作新建跳转 | P1 | 通过 |
| N8 | 静态契约：poll 内接线 detectNewItem + openDrawer；switchProject / btnInit 重置 knownIds | P1 | 通过 |

人工验收（浏览器）：

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| M1 | 看板开着，终端 /req 创建新需求 → ≤1 个轮询周期内看板自动弹出该单详情并有定位提示；/bug 同理 | P0 | 待人工 |
| M2 | 网页「＋ 新建」创建 Bug → 弹窗关闭后直接进入新单详情 | P0 | 待人工 |
