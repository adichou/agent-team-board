# 测试用例 — REQ-20260906-003 Codex 后台自动派发：每单独立会话、日志追踪与进程回收

> 2026-09-06 独立复测：**验收不通过**。下表保留实施时记录，不能作为当前总体验收结论；本次失败、复现脚本及未验证边界见 [最新测试报告](test-report.md)。

状态：假 CLI 自动化已执行（六套测试：dispatch-store / codex-adapter / execution-verifier / scheduler / dispatch-api / codex-ui，全绿）；真机静态预检通过（codex-cli 0.153.1）。真实 Codex 连续执行与取消/恢复（C22/C23）待人工在隔离项目见证，未执行前不称为已支持真实无人值守。

| 编号 | 场景与可检查结果 | 优先级 | 方法 | 结果 |
| --- | --- | --- | --- | --- |
| C01 | CLI 缺失/不可执行/参数不兼容、模型或插件不可用，开关不误显示可运行 | P0 | 假 CLI + 预检 | ◐ T2/D13/A7（CLI 缺失门槛与启动失败）；模型/插件可用性由「验证模型可达」按钮真机验证（留人工触发） |
| C02 | 默认关闭；开启后选单；空队列不调用模型；后来接受条目可被处理 | P0 | 服务集成 | ✅ D2/D3 + T3（假 CLI 端到端：选单、空转不调模型、后接受续派） |
| C03 | 排序、依赖、submitted/done 排除正确；依赖阻塞与空队列区分显示；项目路径别名不绕过互斥 | P0 | 共用数据测试 | ◐ D1+D18+P1/P2（排序/依赖/排除；全部候选依赖未满足时 waiting=deps-blocked 并指出前置，BUG-20260906-005 修复）；路径别名互斥靠服务端 realpath 归一（multi-project 既有覆盖），未单列用例 |
| C04 | Codex、Zcode、手工 claim 同时争抢，最多一个实施；不同项目全局串行 | P0 | 多进程并发 | ✅ D10/D12/D15 + K1（impl.lock 原子互斥 + 全局串行 + 手工认领被拒） |
| C05 | 带空格、单引号、换行、美元符号、反引号的路径/提示词只作为数据；无 open/.command/Terminal | P0 | 参数与真实 shell 注入探针 | ✅ A1/A2（参数数组 + 特殊字符注入探针，提示词经 stdin 纯数据） |
| C06 | 连续多项得到不同 threadId；同项恢复只用已记录 ID，不依赖 --last 或 fork | P0 | 假 CLI 与真实验证 | ✅ A4 + D4（新会话各异；恢复只用记录的确切 threadId） |
| C07 | JSONL 分片、尾行、未知事件、非 JSON 错误、超长输出不使服务崩溃或死锁 | P0 | 流式输出故障注入 | ✅ A3（跨 chunk/非 JSON/未知事件/超长行/无尾换行） |
| C08 | 退出 0 无 report、旧 report、owner 不符、runId 不符均不成功；合法新上报才可进入收尾 | P0 | 证据核对 | ✅ V1–V6 + D5（退出 0 无上报不成功；--run 关联与时间证据双查） |
| C09 | 仅一轮结束但未完成时指定 ID有限续跑；轮数/总时限达到后停止 | P0 | 假 CLI | ✅ D4/D5（默认最多追加 2 轮；超限转 blocked 不再派同项） |
| C10 | 关闭开关不取消当前，停止当前先显示停止中，确认后才显示中断 | P0 | UI + 进程集成 | ✅ D6/D7 + T4（关闭不取消当前；停止中持久化；确认后 interrupted） |
| C11 | 正常退出、启动失败、超时、忽略 TERM 的子进程均按归属回收；其他 Codex 进程不受影响 | P0 | 受管进程树测试 | ✅ A5/A8 + D7（组身份回收、忽略 SIGTERM 强杀、逃逸组不误杀） |
| C12 | CLI 已退但子工具继续延迟写文件/daemon turn 活跃，保持占用并准确停止或 needs_attention | P0 | 工具存活与文件探针 | ✅ A8（同组残留工具组回收+marker 停写验证；逃逸/共享 daemon 不误杀，见 design 9.3） |
| C13 | PID 复用、过期锁、无原生 ID时不误杀不抢占；cleanup_pending 时不派下一项 | P0 | 身份/恢复故障注入 | ◐ A11 + recover PID/启动时刻核对（复用禁止实现于恢复路径）；cleanup_pending 不派下一项有实现，未单列故障注入用例 |
| C14 | 临时网络故障退避且次数有上限；认证/额度错误暂停，不领取其余任务 | P0 | 失败分类 | ✅ D8/D9（认证/额度暂停执行器；网络退避重试有持久化记录） |
| C15 | 在预留、启动、写 PID、写 ID、claim、report、收尾各窗口崩溃，恢复不重复实施 | P0 | 分阶段 kill/重启 | ◐ D11/D11b（reserved/running 窗口恢复 + 重启策略）；其余窗口（写 PID/写 ID/report 后）未做全矩阵 kill 注入 |
| C16 | 重启自动继续默认关；开启后也须核对通过才能继续；重复事件只计一次 | P0 | 持久化配置与恢复 | ✅ D11/D11b（默认等待人工；开启且核对通过才自动继续） |
| C17 | 失败遗留用户改动不会被自动清理；未知安全性时暂停项目 | P0 | 工作区保护 | ✅ D14（git 脏工作区暂停、不清理改动；非 git 项目如实跳过） |
| C18 | 浏览器关闭不停止独立服务；Electron 自建服务协调收尾，复用外部服务不误杀 | P0 | Electron 生命周期集成 | ✅ T5/T6 + electron stopService（SIGTERM 协调收尾、复用不误杀；浏览器关闭不停止独立服务为结构性保证） |
| C19 | 看板展示每项日志/最终回复/报告；大日志分页增量，无 XSS，磁盘写失败不假成功 | P0 | API + 浏览器 | ✅ T3 + U5/U10（账本/最终回复/报告查看、分页与字节偏移增量、汇点转义）；磁盘写失败暂停未自动化 |
| C20 | 未接通桌面入口明确不可用；有 threadId 不误显示侧栏可见；不降级打开终端 | P1 | 能力状态 UI | ✅ U6（桌面入口如实禁用；无会话 ID 不误显可见） |
| C21 | 窄屏面板、开关、配置、取消、失败恢复和项目切换可操作且无重复启动 | P1 | 浏览器验收 | ◐ 浏览器实测面板可用（窄视口下顶栏按钮裁剪属在办 REQ topbar-overflow，另行处理） |
| C22 | 真实隔离项目连续 3 项：不同会话、正确报告、全过程零新增 Terminal 窗口 | P0 | 真实 Codex 人工见证 | ⏳ 待真实隔离项目人工见证（本会话未消耗真实模型请求，不假称已支持无人值守） |
| C23 | 真实取消/恢复：停止后无延迟写入、无误杀，恢复同一项可继续并完成上报 | P0 | 真实进程/会话验证 | ⏳ 待真实进程/会话取消/恢复验证 |
| C24 | 原单项终端派发、状态守卫、report、Electron 复用和默认端口行为无回归 | P0 | 相关现有检查 | ◐ 全量回归通过；detail-close-btn T2 失败属 REQ-20260906-005 会话在途工作，非本需求引入 |

报告至少保存版本、运行配置（去除敏感值）、项目/执行/会话关联、事件样本、测试输出、进程归属与停止证据、看板展示截图或记录。C22/C23 未执行时不得称为已支持真实无人值守。
