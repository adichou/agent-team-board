# 测试用例 — REQ-20260906-002 Zcode 批量实施：轻量主调度、每单新子 Agent 与文件化回执

> 2026-09-06 独立复测：**验收不通过**。下表保留实施时记录，不能作为当前总体验收结论；本次失败、复现脚本及未验证边界见 [最新测试报告](test-report.md)。

状态：用例设计，全部未执行。开发时按 skill 先跑红再实现，真实 Zcode 测试与假执行器测试分开报告。

| 编号 | 场景与可检查结果 | 优先级 | 方法 | 结果 |
| --- | --- | --- | --- | --- |
| Z01 | 混合 req/bug，按类型、创建时间及编号稳定选单；只选 accepted | P0 | 临时项目数据测试 | 通过 |
| Z02 | 批次冻结后新增 accepted，不进入旧批次；上限、空态、仅阻塞项都正确收尾 | P0 | helper 集成 | 通过 |
| Z03 | 依赖不存在、自依赖、环被拒；依赖未 done 时受阻，父子归属不自动阻塞 | P0 | 数据及 UI | 通过 |
| Z04 | 同时启动两批、Codex runner 与手工 claim，最多一个实施任务；项目别名路径不能绕过 | P0 | 多进程并发 | 通过 |
| Z05 | 预留后认领前状态改变或进程退出，不重复实施，不错误重置状态 | P0 | 故障注入 | 通过 |
| Z06 | 复制失败后重新复制同一批次；待启动不误显示执行中 | P1 | UI 与接口集成 | 通过（UI 实测含剪贴板失败恢复路径） |
| Z07 | 假 worker 连续处理 10 项，每项一个新实例且只认领一单 | P0 | 假执行器端到端 | 通过（假 worker） |
| Z08 | 主输入不含全队列/代码/日志；回执、核对各不超过 2 KiB；长错误仍保留完整文件 | P0 | 载荷捕获 | 通过（假 worker 载荷） |
| Z09 | 缺字段、错 ID、未知状态、超长回执被拒；重复回执不重复计数 | P0 | 协议测试 | 通过 |
| Z10 | 旧 report、旧 agentCompletedAt、错误 owner、未上报但返回 reported 均不能通过核对 | P0 | 证据一致性 | 通过 |
| Z11 | report 后开始下一单，不等 done；新状态仍为 in-progress，人工完成权限不变 | P0 | 状态机与守卫 | 通过 |
| Z12 | 认领冲突换单；信息缺失记阻塞；环境错误停止批次而不领取其余项目 | P0 | 故障注入 | 通过 |
| Z13 | 失败遗留改动不确定时停止；不能自动 reset/clean/stash 或删除用户改动 | P0 | 工作区保护 | 通过 |
| Z14 | 暂停后续不领取下一单；原生工具未停时不显示已中断或放开锁 | P0 | 生命周期模拟 | 通过 |
| Z15 | 主会话退出/压缩后续接，已上报项不重跑，未知旧 worker 不被抢占 | P0 | 恢复故障注入 | 通过 |
| Z16 | 360 px 窄屏及常规桌面宽度下入口、抽屉、表格、依赖编辑可操作且无重叠 | P1 | 浏览器验收 | 通过（浏览器实测 541/360px；360px 截图因 IAB 捕获故障缺，几何校验替代） |
| Z17 | 10 项模拟批次与完整日志回传基线比较，记录主会话可观测输入输出字节及差异 | P1 | 测量，不虚构 token | 未验证（需 10 项模拟批次的基线对比实验，见报告） |
| Z18 | 真实 Zcode 隔离项目连续完成 3 项：一次手动启动、逐项新子 Agent、独立记录、正常停止 | P0 | 真实人工见证 | 未验证（需真实 Zcode 主会话+子 Agent 人工见证，见报告） |
| Z19 | 真实启动时记录父历史/AGENTS/工具注入的可观测边界，报告与提示词保证一致 | P1 | 真实检查 | 未验证（随 Z18） |
| Z20 | 现有单项 dev、dev loop、手动派发和状态守卫回归通过 | P0 | 现有相关检查 | 通过（detail-close-btn T2 为 REQ-20260906-005 预置红灯，先于本需求存在） |

验收证据至少包括测试项目路径、工具/插件版本、批次与运行标识、实际工具调用记录、报告路径、测试输出和未验证项。假 worker 不能替代 Z18 的真实验证。

## 实施映射（2026-09-06 实施时补充）

| 测试文件 | 覆盖用例 | 说明 |
| --- | --- | --- |
| `scripts/tests/batch-core.test.mjs` | Z01 Z02 Z03 Z05 Z09 Z10 Z11 Z12 Z14 Z15 | 临时项目内直接驱动 `scripts/lib/batch.mjs` + `core.mjs`：选单排序、批次冻结、依赖校验/阻塞、预留故障、回执协议、证据一致性、report 后续派、冲突换单、暂停、摘要续接 |
| `scripts/tests/batch-cli.test.mjs` | Z04 Z06(接口) Z07 Z08 Z12 Z13 Z20 | 临时项目端到端（子进程 `atb.mjs`）：并发互斥（含别名路径）、假 worker 连续 10 项、主会话载荷 ≤2 KiB、失败/环境错误语义 |
| `scripts/tests/batch-serve.test.mjs` | Z02 Z03(UI 接口) Z06 | 随机端口起服务：批次接口绑定 project、幂等创建、策略校验 400、跨项目账本隔离 |
| `scripts/tests/batch-ui.test.mjs` | Z06 Z16(静态) | 前端静态契约：入口按钮、抽屉结构、待启动/执行中文案、重制复制不建新批次、依赖设置、≤720px 窄屏样式 |
| 真实浏览器走查 | Z16 | 桌面与 360px 视口实测抽屉与依赖设置 |
| 未在本期自动化 | Z17 Z18 Z19 | 需真实 Zcode 主会话/子 Agent 人工见证，报告中标「未验证」 |

补充用例（实施中新增）：

| 编号 | 场景与可检查结果 | 优先级 | 方法 | 结果 |
| --- | --- | --- | --- | --- |
| Z21 | `atb report` 带 `--run` 写入 lastReport.runId；无 --run 时旧调用兼容（runId 为 null 不影响现有流程） | P0 | core 集成 | 通过 |
| Z22 | impl 互斥锁被批次持有时，手工 `atb claim` 被拒并提示核对；锁属主本人 claim 放行 | P0 | core 集成 | 通过 |
| Z23 | `batch next` 返回的单项规范不含批次其余候选清单（主/worker 输入不泄漏全队列） | P0 | 载荷检查 | 通过 |
| Z24 | worker 认领冲突 → `run release` 释放预留；条目仍 accepted 可被他人领取；已认领条目 release 被拒 | P0 | 故障注入 | 通过 |
