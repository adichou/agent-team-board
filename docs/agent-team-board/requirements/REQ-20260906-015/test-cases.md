# 测试用例 — REQ-20260906-015 支持全局搜索，可以任意搜索需求，bug 和文件数中的文件。在需求看板界面就是搜索需求、bug，设计文档，在文件看板就是搜索文件

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| S1 | `GET /api/search`（q 为空/缺失/纯空白）→ HTTP 200，items/docs/files 均为空数组 | P0 | 通过（自动化） |
| S2 | 搜索单号片段（如 `REQ-`）命中需求与 Bug：items 含 id 与 title，大小写不敏感（`req-` 同样命中） | P0 | 通过（自动化） |
| S3 | 按标题关键词命中对应条目，不相关条目不出现在 items | P0 | 通过（自动化） |
| S4 | 按文档正文关键词命中：docs 含 `{id, name, line, text}`，line 为首个匹配行号、text 含关键词 | P0 | 通过（自动化） |
| S5 | 文档首行 H1（含单号与标题的重复元信息）不算内容命中：搜单号时 docs 不把该条目 README 顶行顶出，正文出现才算 | P1 | 通过（自动化） |
| S6 | 文件名搜索：files 含项目内相对路径，basename 大小写不敏感子串命中 | P0 | 通过（自动化） |
| S7 | 文件搜索不进入 node_modules/.git/隐藏目录（在其中放置匹配名文件不出现在结果） | P0 | 通过（自动化） |
| S8 | 每类结果上限 50 条且响应带 truncated 标记（创建 60 个匹配文件验证截断） | P1 | 通过（自动化） |
| S9 | 未初始化看板的临时项目：/api/search 仍可搜文件，items/docs 为空数组 | P1 | 通过（自动化） |
| U1 | index.html 顶栏含全局搜索输入框（.global-search/#searchInput），位于 .top-actions 内 | P0 | 通过（自动化） |
| U2 | app.js 输入防抖 250ms 调 `/api/search`，seq 竞态防护（旧响应不覆盖新输入） | P1 | 通过（自动化） |
| U3 | 需求视图按 items id 集合过滤看板卡片，列签名包含 q（轮询不重置过滤）；docs 命中渲染到文档命中条带并可点击定位文档 | P0 | 通过（自动化） |
| U4 | 文件视图 files 命中渲染为结果条带 chip，点击调用 openFile | P0 | 通过（自动化） |
| U5 | setView 同步 placeholder（两种文案）；输入框内 Esc 清空搜索且不触发全局 Esc 关抽屉 | P1 | 通过（自动化） |
| U6 | style.css 含 .global-search 与结果条带样式，窄屏（≤640px）不破版（沿用 topbar wrap 降级） | P1 | 通过（自动化） |
| M1 | 浏览器人工验收：输入体验、清空恢复、结果点击跳转（无法自动化的视觉/交互项） | P1 | |
