# REQ-20260901-005 认领者统一显示会话名（不再缺省 terminal）

- 状态：submitted（待人工接受）
- 创建：2026-09-01T04:00:02.525Z

## 描述

问题：认领者显示三种来源混杂——ZCode 会话缺省显示 terminal（已验证：Bash 环境未注入 ZCODE_SESSION_ID，core.mjs 的 actor() 兜底为 terminal）、本会话手动传的 loop-session、Codex 会话自觉传的语义名 codex-dev-loop-20260901。用户期望统一为会话名。方案方向（对齐时定）：A) claim 强制显式命名——未传 --by 时报错并提示起一个会话名（如 zcode-视图片面、codex-dev-loop 这类语义名），配 skill 文档统一格式约定；B) 生成可读缺省名（工具名+日期+短随机后缀）兜底，--by 优先；C) 尝试自动获取会话标识（当前 ZCode Bash 环境拿不到，需客户端注入，短期不可行，仅作远期）。推荐 A+B 组合。验收：1) 新认领的条目 owner 均为会话名（语义名或可读缺省名），不再出现裸 terminal；2) skill 与命令文档写明会话名格式约定（两端一致）；3) 已有条目的历史显示不强求迁移（保留原值，历史可追溯）；4) 认领锁的 owner 语义同步；5) 契约测试覆盖缺省命名与显式命名两条路径。

## 验收标准

- [ ] （待补充）
