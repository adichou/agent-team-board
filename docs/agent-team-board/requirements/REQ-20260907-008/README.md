# REQ-20260907-008 讨论视图精简：删除头部说明区，状态筛选与需求栏样式统一并去掉「全部」

- 状态：submitted（待人工接受）
- 创建：2026-09-07T15:17:59.757Z

## 描述

## 背景与目标

讨论视图（Oncall）头部保留了大段说明文字与「＋ 新建讨论单」按钮，占约两行高度；顶栏已有统一「＋ 新建」入口（REQ-20260907-004，含需求 / Bug / 讨论单三类），头部按钮冗余。讨论视图状态筛选条样式（chip oc-filter，计数为纯文本、active 用 inset 描边）与需求栏的 filter-chip 样式不一致。本需求精简讨论视图。

## 期望行为

1. 删除讨论视图头部（「讨论」标题、说明文字、「＋ 新建讨论单」按钮整块移除）；新建讨论单统一走顶栏「＋ 新建」（类型选讨论单），全视图新建入口唯一。空态引导文案改为指向顶栏「＋ 新建」。
2. 状态筛选条改用与需求栏完全一致的样式（filter-chip + filter-count：计数独立 span、active 用 viewrail-accent 描边与浅底色），弃用 oc-filter 的 inset 描边样式。
3. 去掉「全部」筛选 chip：保留 待回复 / 回复中 / 已回复（存在失败单时保留「失败」chip），缺省选中第一个筛选「待回复」；与需求栏去除「全部」的既有口径（REQ-20260907-006 + BUG-20260907-016）保持一致。
4. 删除头部后视图顶部从筛选条开始，与需求视图结构对齐；窄屏与深浅色主题下无回归。

## 验收要点

- 讨论视图无头部说明区；新建入口仅顶栏「＋ 新建」一处。
- 筛选 chip 视觉与需求栏一致（含计数样式与 active 态）；「全部」chip 不存在，缺省选中「待回复」。
- 各筛选计数正确并随 2 秒轮询刷新；空态文案指向顶栏「＋ 新建」。
- 现有 oncall 与看板筛选相关测试无回归，按需补充 UI 契约测试。

## 关联

- 调整 REQ-20260907-001（Oncall 咨询看板）交付的视图结构；布局基线来自 REQ-20260907-004（统一导航与三类新建入口）。
- 「全部」筛选去除口径与 REQ-20260907-006、BUG-20260907-016（需求栏侧）一致。

## 验收标准

- [ ] （待补充）
