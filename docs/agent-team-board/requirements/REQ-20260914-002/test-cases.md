# 测试用例 — REQ-20260914-002 分支浏览中支持在当前分支中搜索提交记录

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

用例分组：D* 数据层/服务接口（build-serve.test.mjs S12 系列）、U* 前端行为（build-ui.test.mjs N7g/N7i 系列）、I* i18n（N7h）。
测试数据口径沿用 S8b：long 分支 64 个提交（init + feat + bulk 62），跨页命中的关键词取 `bulk`（62 条命中，>1 页）与旧位置单号（默认分页第 2 页以远）。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| D1 | 服务端 q 命中跨页提交：`q=bulk` 返回 total=62、默认页 50 条、全部 subject 含 bulk；`limit=20&offset=40` 切片正确（bulk 22→bulk 3，新→旧） | 高 | 通过（build-serve S12「D1」） |
| D2 | 匹配口径四字段：subject 命中（旧位置单号只出现在默认分页第 2 页以远仍命中）、author 命中（`q=T` 全量 64）、短 hash 前缀恰 1 条、完整 hash 前缀命中同一条 | 高 | 通过（build-serve S12「D2」） |
| D3 | 大小写不敏感：`q=BULK` 命中小写 subject 62 条；`q=t` 命中大写作者名 T 64 条 | 高 | 通过（build-serve S12「D3」） |
| D4 | 无命中：200 + commits=[] + total=0 | 高 | 通过（build-serve S12「D4」） |
| D5 | q 缺省 / 空串 / 连续空白均走默认分页（total=64 全量口径，不带 query 回显） | 高 | 通过（build-serve S12「D5」） |
| D6 | 非法输入：非法 ref 注入 400；不存在分支「分支不存在」口径不变；limit=-5 归一为 1；超长 q（250 字符）截断为 200 不报错 | 高 | 通过（build-serve S12「D6」） |
| D7 | 非 git 仓库搜索报错口径同 branchLog（400 + git 仓库引导） | 中 | 通过（build-serve S12「D7」） |
| U1 | 搜索行渲染：未选分支无搜索行；选中后出现输入框（placeholder）+ 搜索按钮；无关键词不出清除入口；默认列表无命中计数行 | 高 | 通过（build-ui N7g「搜索行渲染」） |
| U2 | 提交搜索：回车 / 点按钮同路径发起，请求回第一页带 `&q=`；命中计数「共 N 条匹配（关键词：xxx）」；作者 / 说明命中词 `<mark>` 高亮；沿用既有列表行与分页条 | 高 | 通过（build-ui N7g「q 透传 / 计数 / 高亮」） |
| U3 | 跨页命中：只出现在默认分页第 2 页以远的关键词（合成旧提交 #120 的 REQ-OLD-1201）能搜出并展示 | 高 | 通过（build-ui N7g「跨页命中」） |
| U4 | 无匹配空态：「没有匹配的提交（关键词：zzz）」+ 一键清除；不出现「该分支暂无提交」 | 高 | 通过（build-ui N7g「无匹配空态」） |
| U5 | 清除恢复：清除后请求不带 q、回默认全量列表第一页（第 1–50 条 / 共 137 条）；空白关键词提交等同清除 | 高 | 通过（build-ui N7g「清除恢复」） |
| U6 | 切换分支重置：换分支清空关键词回第一页默认列表；同分支重载（doSync 同名链路）保持关键词重查；「刷新」保持关键词重查 | 高 | 通过（build-ui N7g「切分支重置 / 同分支重载 / 刷新」） |
| U7 | 搜索态分页：翻页 offset 跟随且带 q、末页反馈「已到末尾 · 共 68 条匹配」、每页条数切换回第一页；翻页失败保留已加载内容 + 行内错误 + 重试（重试带 q） | 高 | 通过（build-ui N7g「搜索态分页 / 失败重试」） |
| U8 | 防重复触发：执行中按钮与输入框禁用、「搜索中…」指示、重复提交不产生新请求；与顶部模块搜索互不串扰（setQuery 只影响版本列表，不注入提交搜索） | 中 | 通过（build-ui N7g「防重复 / 互不串扰」） |
| I1 | i18n：新增文案入 EN（搜提交说明 / 作者 / hash…、搜索、清除、搜索中…）与 EN_DYNAMIC（共 ◇ 条匹配（关键词：◇）等 3 条），值无中文、静态键无重复 | 中 | 通过（build-ui N7h + i18n-dict D1e 全量校验） |

实施说明：

- 静态契约（绑定与样式）由 build-ui N7i 覆盖（#bldLogSearchInput / #bldLogSearchGo / data-log-search-clear 绑定，.bld-log-search / .bld-log-count / .bld-log li mark 样式）。
- 新增用例先跑红（S12 断言 `q=bulk 命中 62` 失败、N7g/N7h 失败）再实现跑绿；既有 14 + 1 用例（build-ui / build-serve）全程保持通过，`npm test` 233 个测试文件 0 失败。
