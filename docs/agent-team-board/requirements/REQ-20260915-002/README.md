# REQ-20260915-002 支持从已合并的版本计划发起跨仓库产品发布并跟踪发布结果

## 背景与已核实基线

用户已具备构建模块中的版本计划并完成 main 合并。现有 REQ-20260913-001 已为 done；本地 main 有 BLD-20260914-001 的合并记录（登记时 tip 为 4d044f1，仅为观察证据，不作为未来默认发布提交）。现有 build-store/build-git 支持 BLD 草稿、合并和逐项结果，release-store 及 Git/Apple/Electron 执行器已存在。

发布时产品源码仓库必须切换到 `main` 分支，以经过预检并冻结的 `main` 提交发布，同时将 `main` 和 `dev` 两个分支一起推送到该源码仓库的远端。这是本需求新增的目标行为，不代表当前已实现。

本需求在既有构建与发布模块上增量打通产品发布，不重新实现版本计划或重复合并 main。以 agent-team-board 验证 Web App 与官网/用户文档发布流程；本次仅讨论并完善需求，不执行真实发布。

## 依赖与边界

- REQ-20260915-001：产品标识、官网仓库职责、文档与引用规范；本需求仅依赖其中与 Web App 和官网/用户文档发布相关的约定，不以私有运营资料、博客或社区推广准备为前置。
- REQ-20260913-001：现有构建与版本计划；REQ-20260910-029：现有发布模块。
- 构建计划保留 merged 状态，发布运行独立建模并关联 BLD；发布不自动更改关联 REQ/BUG 的人工验收状态。
- 本期软件交付新增 Web App 支持，包含产品源码切换 main 与 main/dev 双分支推送步骤，暂不扩展独立 Git 发布渠道及 Electron/Apple 执行器；既有执行器的存在不代表本期需接入这些渠道。
- 私有运营资料准备、博客与社区推广不在本次需求范围内，不进入发布阶段、目标选择或完成判定。
- 发布 Web App 与公开源码是不同目标。源码私有仍可部署本机可访问的 Web App 与官网，绝不因此公开源码或改变仓库可见性。

## 流程与发布内容

### 1. 从版本计划创建发布

构建详情中已 merged 版本显示「创建发布」；未合并版本说明前置条件。进入发布模块创建草稿，自动带入 product_id、BLD ID、版本名称/描述、关联条目与 commit、合并证据，不要求重复选单。版本显示名和实际发行版本号分开。已有运行可查看；同目标活动运行禁止重复创建执行。

### 2. 冻结范围和预检

记录确切的发行版本号、源码提交 SHA、关联条目快照、目标仓库/分支、材料清单与内容指纹。发布源码 SHA 必须与本次冻结的 main 分支头一致，并验证其包含计划条目；不能直接用执行时最新 main 替换计划结果。老计划无合并结果 SHA 时，展示候选、main 额外提交及来源证据供明确选择；不能声称发行仅包含所选条目而隐去合并带入的额外变更。

若 main 已在计划合并后前进，必须展示额外变更，将实际 main 分支头纳入发布范围并重新冻结、预检，不能把旧计划 SHA 冒充当前 main 发布。同步冻结 dev 分支头和产品源码远端目标；main/dev 任一分支或远端目标变化均使旧预检失效。

预检检查产品映射、源码可见性、目标权限、版本冲突、构建/测试、材料与链接、可公开文件范围。未提交修改不自动提交/暂存/覆盖；在隔离目录验证冻结源码。输入或材料变化使旧预检失效，需重新展示具体发布计划。

### 3. 软件与版本交付

用户启动已预览的发布计划后，先将产品源码工作目录切换到 `main`，核对 HEAD 等于冻结的 main SHA，再将冻结的 `main` 和 `dev` 一起推送到同一个产品源码远端，分别更新远端同名分支。远端从仓库已有配置自动解析并展示于计划，缺失或存在歧义时明确阻塞；不推送到官网仓库，不自动合并 dev 到 main，也不改变仓库可见性。构建只使用冻结的 main 源码，不能从 dev 发布。发布执行后源码工作目录保持在 main。

预检检查两个本地分支存在、工作目录干净、main 可切换、远端权限及两个分支可正常推送。未提交改动或分支被其他工作树占用时，说明阻塞原因，不自动暂存、丢弃改动或强行切换。执行前再次核对冻结输入；采用一次包含 main/dev 的原子推送，远端不支持原子推送或任一分支被拒绝时阻塞，不强推、不回退为可能仅成功一个分支的推送。推送后核验远端两个分支 SHA 与冻结值一致，双分支同步成功后才继续构建部署；失败或超时记录真实结果，重试前先查询远端，不把仅单分支成功视为完成。

本期新增 Web App 本机发布支持：系统检查冻结源码的框架、项目架构、包管理器、脚本与框架配置，自动生成构建命令，自动配置产物目录、本机部署环境与部署方式，无需用户填写。无需独立构建步骤的项目按其运行架构自动处理，不强制套用 `npm run build` 或 `dist/`。基于冻结源码执行构建与验证，记录实际版本、源码 SHA 与产物摘要，自动分配本机端口并生成本机访问地址，部署后回验页面可用性和版本标识；不需要公开访问 URL，不配置公网部署。源码私有时，本机运行不得依赖浏览器访问私有仓库。自动识别失败时展示具体原因和诊断信息，不把手填构建参数作为正常流程，也不猜测执行。

产品源码 main/dev 双分支推送纳入本期发布验收；暂不扩展独立 Git 发布渠道、标签/Release、Electron 安装包或 Apple 商店交付。版本号修改如需源码提交，应在最终冻结与验证前完成，禁止已验证 SHA 与实际构建源码不一致。Web App 部署方式缺少执行支持时明确阻塞/待外部操作，不把生成命令当执行成功。

### 4. 官网与用户文档发布（必备目标）

官网仓库根目录在设置模块中按用户一次设置，供后续项目构建与发布复用，不硬编码 `app-homepage-repo` 或固定绝对路径。首次未设置时，创建入口引导到设置页面，保存后返回原发布流程并保留草稿；后续无需每次设置。产品内容子目录自动使用当前项目名（本项目为 `agent-team-board/`），官网目标分支默认为 `main`，均无需用户关注或填写。项目名必须校验为安全子目录，不允许路径越界；无效或冲突时给出明确诊断。目录配置保存在本地不跟踪或私有配置中，创建草稿时自动带入，冻结时记录实际目标；设置中的路径变更使旧预检失效。预检校验目录存在、可访问、仓库及目标分支有效、产品内容路径位于配置的仓库内；未配置或配置无效时阻塞并提示设置入口。

在配置的官网仓库及产品内容目录中更新产品信息、Web App 访问/使用方式、用户指南/FAQ、公开更新日志与版本截图；上述官网内容提供中文（简体）与英文两个版本，包含导航、按钮、截图说明及替代文本，两种语言的功能描述和发行版本保持一致。检查源码 README 的人/Agent 入口和公开互引是否需更新。按版本条目生成可编辑草稿，使用用户可理解的变化，不原样发布内部需求报告。冻结官网提交与软件版本关联，自动识别站点构建方式并部署到本机，回验本机页面、版本标识、Web App 入口和文档链接。源码保持私有时官网仍可在本机独立访问与构建。允许先部署无新版 Web App 入口的准备页面；正式新版入口须在 Web App 部署并回验成功后启用。

官网默认按浏览器语言自动选择：在没有手动语言偏好时，按浏览器语言优先级依次匹配，`zh` 及其地区变体使用中文，`en` 及其地区变体使用英文；无匹配语言或无法读取时回退英文。官网页头提供「中文 / English」切换入口，窄屏同样可见，明确标识当前语言。手动切换后保留当前产品、文档及版本上下文，并在本机记住选择，后续访问优先使用该选择；提供「跟随浏览器」选项以恢复自动选择。浏览器存储不可用时，切换仍在当前页面生效。

发布草稿同步准备中英文材料，预检检查两种语言的内容完整性、版本一致性与对应页面链接；缺少任一必备译文时列出缺失页面并阻塞官网发布。部署后分别回验中英文页面、语言切换与文档链接，两种语言均通过才将官网目标标记为完成。

### 5. 结果核验与发布记录

分开显示 Web App 已发布、官网已上线。产品源码 main/dev 双分支推送核验成功且两个必备目标均完成回验才显示整体已发布；部分成功保留各目标真实状态。保存发布前后源码分支、远端目标、main/dev 冻结 SHA 与推送核验结果、版本、产物摘要、执行日志、部署运行标识和自动生成的本机链接，私有记录保留完整信息，公开报告只导出允许字段。部署命令成功不等于站点上线，需以本机入口和版本回验结果为准。

## 界面布局、交互与反馈

- 构建版本详情操作区新增「创建发布」「查看发布记录」，保留原有合并功能；未 merged 禁用创建并说明原因。
- 发布模块沿用左运行列表/右详情。列表显示产品、发行版本、来源 BLD、整体状态；详情顶部展示冻结 SHA 与版本范围，下方「Web App / 官网文档」两个必备目标卡。
- 创建面板预填 BLD 信息，仅需核对发行版本号；构建命令、产物目录和本机部署环境自动识别配置，产品内容子目录自动取项目名，目标分支默认 `main`。面板不提供上述技术参数或公开访问 URL 输入框，官网仓库根目录仅从设置模块读取；首次缺失显示「前往设置」，保存后返回继续。可保存草稿与预检。预检不推送、不上传、不部署、不发帖。
- 「预览发布计划」集中展示源码切换到 main、将一起推送的 main/dev SHA 与远端目标、实际会更新的仓库/分支、文件差异、Web App 构建产物、自动配置的本机环境和本机入口；用户启动即授权所展示的明确操作，不对已授权的相同步骤重复确认。变更发布范围后需重新确认新计划。
- 详情提供「概览 / 材料与差异 / 日志与证据 / 发布链接」；阶段可见失败原因、重试、待人工处理入口与取消后续阶段。
- 官网产品页与文档页头提供「中文 / English / 跟随浏览器」语言入口，默认跟随浏览器；切换保留当前文档上下文并反馈当前语言。发布详情的「材料与差异」分别显示中英文材料及缺失项。
- 空态引导选择已合并版本；未配置映射给出具体字段与设置入口；执行中禁用重复操作；失败保留成果与原因；部分上线明确显示各目标状态；深浅色与窄屏遵循现有布局规范。

## 界面展示

[打开单文件可交互演示](./ui-demo.html)。演示数据全部为示例，按钮只改变本地演示状态，不执行构建、部署或访问外部地址。

- 布局：顶部保留来源版本与创建入口；左侧为运行列表，右侧为冻结范围、两个必备目标、详情页签和操作区；窄屏按上下顺序排列。
- 交互：从「创建发布」进入草稿面板；首次未设置官网仓库根目录时先引导到设置页，保存后返回。自动配置项无需填写，保存草稿后预检，再预览明确计划并启动；修改版本或路径使预检失效。演示可切换读取空态、加载、失败、部分上线、完成以及未合并场景，支持失败重试和取消后续阶段。
- 状态反馈：加载时禁用发布操作；读取失败提供重试；未合并禁用创建；配置缺失阻塞预检；部分上线分别保留 Web App 成功与官网失败证据；整体完成仅在两个目标回验成功后展示。失败重试只补官网阶段，取消保留已上线的 Web App。


```text
构建 / 版本计划
BLD-…  已合并 main       [创建发布] [查看发布记录]

发布
运行列表                 │ Agent Team Board · vX.Y.Z · 来源 BLD-…
版本 / 状态              │ 冻结 SHA / 条目范围 / 额外提交提示
                         │ Web App       必备  待预检
                         │ 官网与文档    必备  草稿已准备
                         │ [概览] [材料与差异] [日志与证据] [发布链接]
                         │ [预检] [预览发布计划] [启动] [重试] [取消后续]
```

## 一致性、恢复与私有数据

- 跨仓库发布不宣称原子事务；单目标成功落证据，后续失败保留已上线事实，重试只补未完成操作。取消不撤回已发布内容。
- 超时先查询远端/部署/上传真实结果，再决定重试；服务重启不重复发版；同产品版本目标加互斥与幂等标识。
- 来源计划后来改名或被删除不破坏发布快照及历史；关联悬空时说明来源已删除。
- 发布运行中的私有仓库路径、材料全文与日志放本地不跟踪或私有存储，不因看板源码公开被带出；公开页面和报告不暴露此类数据。

## 验收标准

- [ ] 发布执行自动切换产品源码到 main 并保持在 main；实际构建 SHA 等于冻结 main 分支头，不从 dev 构建。覆盖 main 前进后重新冻结、工作区未提交改动、分支缺失及工作树占用场景。
- [ ] 同一次原子推送将 main/dev 更新到源码远端同名分支，并核验两个 SHA；覆盖远端缺失/歧义、不支持原子推送、权限失败、非快进拒绝、超时核验和重试。任一失败均阻塞后续构建部署，不强推、不误报整体完成。

- [ ] 已合并 BLD 可一键生成发布草稿并双向查看记录；不重复选单/合并，未合并计划有明确阻塞。
- [ ] 冻结 SHA、条目、实际版本及材料指纹可追溯；覆盖 main 前进、旧计划缺失 SHA、额外提交和材料变更场景。
- [ ] 公开与私有源码两种配置均可走完产品发布；私有源码不被公开、Web App 本机入口可访问。
- [ ] Web App 根据代码框架与架构自动填写构建命令、配置产物目录与本机部署环境/方式，自动生成本机地址，不要求公开访问 URL 或手填技术参数，版本与冻结源码及产物来源一致，部署后回验可用性与版本；失败有真实日志，缺少部署能力不可伪报成功。本期包含源码双分支同步，不扩展独立 Git/Electron/Apple 发布渠道。
- [ ] 官网/用户文档/更新日志按清单准备并真实部署，本机地址与版本/Web App 入口回验；独立构建不依赖私有 repo。
- [ ] 官网产品信息、访问/使用方式、用户指南/FAQ、更新日志及截图说明提供完整中英文版本；预检报告缺失译文和版本不一致，部署后分别回验两种语言页面及链接，均通过才完成官网目标。
- [ ] 默认按浏览器语言优先级选择中英文，无匹配时回退英文；覆盖 zh-CN、zh-TW、en-US、多语言优先级与不支持的语言。手动切换保留当前产品/文档/版本，刷新后记住偏好；选择「跟随浏览器」恢复自动选择，存储不可用及窄屏下仍可切换。
- [ ] 官网仓库根目录仅在设置模块一次配置并复用，首次缺失引导设置后返回；产品内容子目录自动取项目名，官网分支默认 main；覆盖不同用户目录、首次未配置、无效路径、项目名越界/冲突及设置变更使预检失效的场景，不依赖固定 `app-homepage-repo` 目录。
- [ ] 发布目标仅包含 Web App 与官网/用户文档，源码切换 main 和 main/dev 双分支同步作为必经前置步骤进入界面与整体完成判定；不包含私有运营资料准备、博客或社区推广。
- [ ] 发布计划可预览并显式启动，预检无外部写入；正确展示空态、阻塞、运行、失败、部分上线和完成。
- [ ] 重试、超时、重启、并发、取消、来源计划修改/删除不导致重复发布或历史丢失；REQ/BUG 人工状态不改变。
- [ ] 用 agent-team-board 样板验证完整流程；测试/演练与真实上线证据分开记录，真实上线需执行具体计划授权。

## 补充核实与待确认

- 代码基线：`scripts/lib/release-store.mjs` 的 `TARGET_TYPES` 当前将 `web` 标记为 `enabled: false`，`scripts/web/release.js` 同样未开放网站入口。本 README 的双目标 Web App 流程是目标行为，不能把现有枚举视作可部署能力。
- `scripts/lib/build-store.mjs` 的 `finishMerge` 当前记录逐项合并时间与错误、版本合并完成时间；该函数未保存最终 main SHA。旧计划候选提交必须补可核验来源，不能用时间戳或登记时 tip 代替冻结证据。
- 自动配置边界：实施时从冻结源码识别 Web App 与官网的框架/架构、构建及运行方式，自动确定产物目录、本机环境、端口和版本探测规则；覆盖无构建步骤的项目。识别失败或缺少执行支持时明确阻塞并给出诊断，不要求用户选择部署环境或提供公开 URL。官网仓库根目录是设置模块的一次性配置；子目录自动取项目名，目标分支默认 main。演示中的识别结果、路径、SHA 与地址仅为示例。
- 待确认：官网截图具体清单与无界面变化版本是否复用旧截图的规则；草稿应标明待补材料，不生成虚假截图。

### 补充验收标准

- [ ] 浏览器直接打开 `ui-demo.html` 无需网络和构建，可操作创建配置、预检、计划预览、模拟启动、部分失败重试及取消；可切换正常、空、加载和失败状态。
- [ ] 修改版本号、目标路径或材料后，「启动」不可沿用旧预检；新计划展示冻结输入后才能启动。
- [ ] 读取失败与部署失败分别呈现：前者重试读取，后者保留已成功目标并只补未完成阶段；取消不将已上线目标改为未发布。
- [ ] 自动识别失败、缺少执行能力或版本回验规则时阻塞，并展示具体诊断；官网根目录缺失引导设置，技术参数不转交用户逐项填写；演示结果不得作为真实上线证据。
