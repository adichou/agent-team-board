# 测试用例 — REQ-20260909-001 任务设置中仅保留对Agent隐藏的配置，其他的全删掉

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| T1 | 渲染精简：taskSettingsHtml 不再出现子代理模型输入（tsModel-）、推理强度选择（tsLevel-）、「子代理模型 / 推理强度」表头与默认智能档建议；保留「批量任务」标题、一句说明（隐藏后，该 Agent 不出现在对应任务的启动选项中）、批量完善/批量开发两组、Codex/Zcode 行、四路 tsHidden 复选框（aria-label 与勾选回显不变）、「执行 Agent / 是否隐藏」两列、保存按钮与就近状态位 | P0 | 通过 |
| T2 | 保存仅提交 agents：保存请求体只含 agents 键（无 models），agents 由复选框反算；不携带已删除控件的空模型/档位值 | P0 | 通过 |
| T3 | 数据层不重置：saveTaskSettings 仅 patch agents 时，既有 models（模型/档位）原样保留并可持久化读取（验收「不重置原模型/档位字段」） | P0 | 通过 |
| T4 | 草稿与就近反馈：勾选变更后状态位显示「有未保存的更改」；保存中按钮「保存中…」并禁用重复提交；保存成功显示「已保存」；保存失败保留草稿、显示错误、不误报成功 | P0 | 通过 |
| T5 | 加载状态：设置视图先渲染「正在加载任务设置」骨架，不渲染隐藏复选框与保存按钮（禁编辑与保存，避免未加载完成覆盖配置）；加载完成后渲染完整表格 | P1 | 通过 |
| T6 | 加载失败与重试：任务设置读取失败显示「任务设置加载失败」错误与「重试」按钮，重试成功后恢复编辑；该提示与全部隐藏提示可区分 | P1 | 通过 |
| T7 | 全部隐藏提示：某类别保存值或草稿全勾选时，该类别显示「无可用 Agent」提示（与读取失败文案不同），取消勾选后提示消失 | P1 | 通过 |
| T8 | 文案与入口精简：设置视图与批量任务分区说明不再出现子代理模型/推理强度/智能档/子代理参数字样；无残留空列或占位 | P1 | 通过 |
| T9 | 底层链路保留：server 派发（/api/batch/create、/api/refine/create）仍按设置注入模型/档位参数，task-settings.mjs 存储结构与校验不变（待确认事项未决前不删底层） | P1 | 通过 |

测试文件：`scripts/tests/task-settings-simplify-20260909-001.test.mjs`（T1–T2、T4–T8 前端 vm 契约 + 行为；T3、T9 含数据层行为与静态链路断言）。
配套调整：`scripts/tests/tasks-panel-26.test.mjs` K10/K11、`scripts/tests/refine-ui.test.mjs` R12-7 由「四列表格」旧契约更新为「两列（执行 Agent / 是否隐藏）」新契约；`scripts/tests/settings-simplify-20260909-002.test.mjs` 的复选框可访问名称契约（T6）保持通过。
