# 测试报告 — REQ-20260904-001 分派给 codex 和 zcode 的提示词增加一句：将当前会话名改为单号

- 时间：2026-09-05T12:39:01.876Z
- 执行者：atb-0905-e41b
- 测试框架：node:assert 静态契约（scripts/tests/dispatch.test.mjs）
- 覆盖率：100%

## 总结

dispatchPrompt 两版提示词各加一句「请将当前会话名改为 <单号>」：codex 版置于句首接原 skill 指引，zcode 版在 /dev 行后追加独立一行（首行仍以 /dev <单号> 开头，自动标题含单号的效果保留）。TDD：dispatch.test.mjs 新增 P1.5 契约用例（断言改名句在模板中恰好出现两次且用 it.id 变量）先跑红，实现后跑绿；全量 20 个测试文件无回归。派发链路（剪贴板/深链/.command）未改动，无新增注入面。M1（真实派发后 Agent 会话名变为单号）待用户手工验收。

## 明细

（可粘贴命令输出、失败用例说明等）
