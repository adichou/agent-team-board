# REQ-20260906-020 待接受卡片「✓ 接受」按钮移至单号右侧

- 状态：submitted（待人工接受）
- 创建：2026-09-06T11:27:43.604Z

## 描述

待接受列卡片的「✓ 接受」按钮目前悬在卡片底部右侧独立一行，与单号分离、视觉跳跃。改为把按钮移入卡片首行（card-top），紧邻单号（.cid）右侧，与详情抽屉「关闭按钮在单号右侧」的既有形态保持一致；原底部按钮行移除。

## 界面布局
- 待接受卡片首行结构：[选择框][REQ/BUG chip][单号][✓ 接受按钮]；按钮紧凑尺寸（padding/字号对齐 12px 级控件），flex:none 不被压缩。
- 卡片底部原 .card-accept 按钮行删除，样式一并清理。

## 交互行为
- 按钮点击行为不变：单条接受（stopPropagation，不打开详情）、接受中禁用、syncAcceptance 同步禁用态。
- 勾选复选框与全选/批量接受操作栏不受影响。

## 状态反馈
- 按钮位置变化不改变任何文案与禁用逻辑；卡片高度因去掉底部行而变矮，列内布局自然收紧。

## 验收标准

- [ ] （待补充）
