# 测试用例 — REQ-20260914-007 人工确认完成和版本合并成功后自动提交管理文件，失败时明确提示并支持重试

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。
> 测试文件：`scripts/tests/mgt-auto-commit-20260914-007.test.mjs`（真实 git 临时仓库 + HTTP 服务端到端，模式对齐 auto-commit-pre-dirty-20260913-006 / batch-serve）。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| P1 | 确认完成入口（API）：in-progress → done 成功后自动提交 status.json 及本次确有刷新的 confirmations.md / decisions.md；提交说明 `doc: 人工确认完成 <ID>` 过规范核验；相关文件不再遗留未提交；账本记录 committed；条目详情接口返回 mgtCommit | P0 | 通过 |
| P2 | 提交纪律：无关已跟踪脏文件、其他条目未跟踪目录、用户预先暂存内容均不被夹带，暂存状态不被破坏；提交仅含目标路径 | P0 | 通过 |
| P3 | 目标文件操作前已脏（无法安全分离）：整操作不提交，返回 failed + pendingManual 文件列表 + 建议；改动保留在工作区 | P0 | 通过 |
| P4 | 版本合并入口（当前分支 dev）：合并成功后 version.json 自动提交到 main（临时工作树，说明 `doc: 版本合并记录 <BLD>`），当前分支不变、不推送；当前分支同内容一并提交，工作区不再遗留版本记录 | P0 | 通过 |
| P5 | 当前分支即 main：原地路径限定提交，不建临时工作树、不切分支 | P1 | 通过 |
| P6 | 幂等：成功后重试 → 已同步（noop）不制造空提交、不重复合并；账本清除失败态 | P0 | 通过 |
| P7 | 失败反馈与重试：Git 身份缺失 → 提交 failed（原因 / 未提交文件 / 建议），条目保持 done；账本持久化，详情接口刷新后仍可见；配置身份后 `POST /api/mgt-commit/retry` 只补交管理记录（不重放确认完成），成功后清除失败提示 | P0 | 通过 |
| P8 | CLI：`atb status <ID> done --json` 结果携带可识别 mgtCommit；`atb mgt retry item <ID>` 补交并输出结果 | P1 | 通过 |
| P9 | 串行与非 git 边界：管理提交经文件锁串行（锁被占返回明确 failed）；非 git 项目返回 skipped 不影响业务流转 | P1 | 通过 |
| P10 | 前端：条目详情完成状态附近与版本详情合并结果区渲染「管理记录提交」反馈块（提交中禁用 / 成功短 SHA / 失败原因+文件+建议+重试按钮），重试入口绑定 /api/mgt-commit/retry，样式沿用深浅色变量 | P1 | 通过 |
