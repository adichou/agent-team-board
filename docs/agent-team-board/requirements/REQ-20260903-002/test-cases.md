# 测试用例 — REQ-20260903-002 看板页面重新设计，现在竖屏操作，每个类别的单只能看见一条，多的就要下拉滚动条，不友好

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。
> P1–P6 为静态契约用例（scripts/tests/portrait-board.test.mjs，含 layout.test.mjs T7 修订）；
> P7 为浏览器实测。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| P1 | ≤1020 断点 `.board` 为 flex 横向滚动容器 + scroll-snap 按列吸附 + 吸附位右移（scroll-padding-left） | 高 | ✅ 静态断言通过 |
| P2 | ≤1020 断点 `.col` 列宽避开左缘 tab 条（flex-basis calc）并按列吸附，超宽封顶 560px 露出下一列预览 | 高 | ✅ 静态断言通过 |
| P3 | 旧降级布局已全部移除：单列四行均分 + ≤1020 的 2×2 网格（v5 废除） | 高 | ✅ 静态断言通过 |
| P4 | 桌面（>1020）`.board` 四列网格不回归 | 高 | ✅ 静态断言通过 |
| P5 | `.board-tabs` 默认 display:none，≤1020 显示为 sticky 左缘竖 tab 条（竖排文字 + 计数） | 中 | ✅ 静态断言通过 |
| P6 | app.js：tab 条注入看板首位、点击平滑跳列（含内核兜底）、scroll/resize 高亮当前 tab、计数随渲染刷新 | 中 | ✅ 静态断言通过 |
| P7 | 浏览器实测：横滑按列切换、tab 高亮随动、列内可纵向滚动、tab 条不被列遮挡、文件视图/桌面隐藏 | 中 | ✅ 390/718/1280 实测通过 |
| P8 | ≤640 顶栏紧凑化 + ≤1020 左侧视图切换：路径隐藏、连接指示恢复、项目切换器自适应、视图 tab 竖排立于左缘顶栏下方（列 tab 按其高度让位、无重叠，文件视图预留左缘宽度，桌面不回归） | 中 | ✅ 静态断言 + 495/1280 实测通过 |
