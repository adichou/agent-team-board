# 测试用例 — REQ-20260910-022 营销与增长：project-growth Skill 与可追溯 AI 策略复盘

> TDD 流程：先在这里列出用例并跑红，再实现代码跑绿。
> 分层：G*（数据层 growth-store）、S*（服务接口）、U*（前端 marketing.js）。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| G1 | 五类入口（positioning/pricing/channels/content/review）创建 agent-run：输入引用及版本来自当前档案（profile@r、实体@r、观察@r；README 与不可变定价版本无版本要求），状态 waiting、保存提示词留档 | P0 | 通过 |
| G2 | 提示词契约：包含项目路径（限定该项目）、营销资料目录、输入引用及版本、目标任务、复盘观察期（review）、输出要求（区分事实/假设、证据引用、候选不自动生效）、保存协议 CLI 命令与草稿 JSON 结构、技能缺失兜底声明（不假称已运行外部 skill） | P0 | 通过 |
| G3 | 回执写入：合法草稿 → run 状态 received、记录来源会话/回执时间/输出摘要，草稿落盘（run.json + draft.md），执行结果 success 入回执日志 | P0 | 通过 |
| G4 | 幂等：相同任务重复提交回执跳过、不重复保存、草稿引用与时间戳不变（内容变化也不覆盖，仅标记 contentChanged） | P0 | 通过 |
| G5 | 过期输入：任务创建后档案并发更新（profile/实验/观察 revision 变化）→ 回执拒绝（stale），不写草稿、不覆盖他人更新，回执日志记录拒绝原因；按当前版本重建新任务输入引用已更新 | P0 | 通过 |
| G6 | 跨项目写入：任务 ID 在本项目不存在 → 明确报错拒绝（不得写入另一项目的 run） | P0 | 通过 |
| G7 | 草稿校验：insufficient=true 时不得携带候选行动、必须给补采建议；字段类型非法（facts 非数组 / 候选缺 kind / kind 非法）→ 定位字段报错，不写盘 | P0 | 通过 |
| G8 | 候选编辑：仅 pending 候选可编辑（title/verify/reason），保存回草稿不改变状态 | P1 | 通过 |
| G9 | 显式采纳—定价：采纳 pricing 候选创建新定价版本（候选不自动成为当前方案，currentPricing 不变）；重复采纳同候选幂等（不重复创建版本） | P0 | 通过 |
| G10 | 显式采纳—定位：采纳 positioning 候选把内容作为「假设」证据追加进档案（revision 递增、来源指向 agent-runs）；档案并发变化时报冲突不静默覆盖 | P0 | 通过 |
| G11 | 显式采纳—渠道/实验/行动/复盘：分别创建 channel（不自动标记已验证）、experiment、activity（草稿，不自动发布）、review（快照固定）；底层校验失败（缺必填）定位字段且候选保持 pending | P0 | 通过 |
| G12 | 保留草稿：候选保持 pending→kept，未写入正式档案；全部候选处理完（采纳或保留）run 状态 done | P0 | 通过 |
| G13 | 接续任务：continueOf 引用历史 run，新任务提示词包含历史草稿与采纳记录引用；未收到回执前读取历史资料可继续（run 可按 ID 读取） | P0 | 通过 |
| G14 | 空项目：营销档案未初始化时仍可创建任务（输入仅 README，无版本要求），提示词声明「档案缺失，仅基于 README 与通用方法」 | P1 | 通过 |
| G15 | 无指标复盘：观察期内无观察记录的 review 草稿以 insufficient 保存（仅补采建议，不生成虚构结论），无候选可采纳 | P0 | 通过 |
| G16 | 读态：readGrowth 两态（未初始化营销 → initialized:false）；run 记录含任务 ID、来源会话、输入引用及版本、输出摘要、草稿引用、写入时间、执行结果；损坏 run.json 只读占位不炸整页 | P1 | 通过 |
| S1 | GET /api/marketing/growth 返回 runs + 技能可用性（未安装：列出能力缺口，不假称已运行） | P0 | 通过 |
| S2 | POST /api/marketing/growth/run 创建任务（201，返回 run+提示词；review 带 from/to 观察期；非法 type/日期 400） | P0 | 通过 |
| S3 | GET /api/marketing/growth/run/:id 详情；不存在 404 | P1 | 通过 |
| S4 | POST adopt/keep/edit：合法 200（返回 run 与写入结果）；校验失败 400 fields；adopt 幂等重复 200 | P0 | 通过 |
| S5 | CLI 回执：`atb growth receipt <id> --file draft.json --dir <root>` 写入成功输出回执 JSON；重复提交幂等；跨项目（错误 --dir）报错；`atb growth show <id>` 可读（跨会话接续） | P0 | 通过 |
| U1 | 源码契约：四页签均有 AI 入口按钮（梳理定位/分析定价/制定渠道计划/生成内容/生成复盘草稿），效果页含「生成复盘草稿」 | P0 | 通过 |
| U2 | 任务面板：选择入口后展示将使用的资料（引用+版本+与当前一致性）、技能缺失能力缺口与兜底说明；复制提示词仅写剪贴板并登记任务，显示「尚未收到结果」 | P0 | 通过 |
| U3 | 草稿面板：回执到达后分区展示事实/假设/待确认、证据与缺失信息，候选逐项 编辑/采纳/保留草稿；数据不足场景显示补采建议且无候选 | P0 | 通过 |
| U4 | 运行记录：任务 ID、类型、输入引用及版本、状态、写入时间、操作（查看/继续任务提示词）；等待回执显示「尚未收到结果」 | P1 | 通过 |
| U5 | 采纳反馈：候选定价采纳后提示「不自动成为当前方案」；保存/采纳失败显示错误并保留草稿；复制继续任务提示词生成含历史引用的新任务 | P0 | 通过 |
