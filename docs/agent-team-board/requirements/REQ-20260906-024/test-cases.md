# 测试用例 — REQ-20260906-024 Codex 派发模型配置：项目默认、单项覆盖、续跑固定与异常提示

状态：已实施（TDD）。规则层与调度/API/UI 用例全部通过；M18 真实模型请求按约定待人工触发，未执行。
测试文件：`scripts/tests/codex-model-config.test.mjs`（规则层）、`codex-model-dispatch.test.mjs`（调度器）、`codex-model-api.test.mjs`（Status Board API）、`codex-ui.test.mjs` U13–U19（UI 契约）。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| M01 | 无新增字段的旧项目默认继承；项目、profile、全局及适用管理配置有不同值时解析符合实际 CLI，无法解析时明确阻止新派发 | P0 | 通过（规则层 M01a–d + 调度 M14） |
| M02 | 项目指定与单项指定同时存在时本项优先；取消本项覆盖后回到项目；不同项目互不影响 | P0 | 通过（规则层 M02 + 调度 M02c + API T2） |
| M03 | 切换模型后原推理强度不兼容，UI 要求重新选择且后端拒绝非法组合；未知能力不伪报已验证 | P0 | 通过（规则层 M03a–d + 调度 M03 + UI U14） |
| M04 | 已创建运行后分别修改全局、项目及单项配置，后续新运行用新值，原运行的续跑和网络重试仍使用原快照 | P0 | 通过（调度 M04a/b：快照落盘 + 续跑轮沿用原参数） |
| M05 | 服务重启恢复原运行，确切会话 ID 和模型/强度不漂移；提供方身份变化时暂停核对 | P0 | 通过（调度 M05：新调度器实例恢复沿用快照；provider 变化拒绝恢复） |
| M06 | 首次 exec 与 exec resume 都收到正确模型及 TOML 强度参数；特殊字符、控制字符、选项形态输入不会注入命令或额外配置 | P0 | 通过（规则层 M06a–d + 夹具 argv 记录断言；新会话与 resume 均含 --model/-c） |
| M07 | 真实可达验证与正式运行使用相同解析结果；验证响应晚于用户改配置时不能覆盖新结果，保存/轮询不启动模型 | P0 | 通过（API T4：验证走同一解析/参数构造、结果绑定指纹持久化、配置变化后指纹不匹配；仅按钮触发） |
| M08 | 模型列表读取失败时给出诊断和手动 ID 入口；列表存在不等于账户可用，不为所有候选模型逐个发请求 | P1 | 通过（API T3 + UI：datalist 手动输入标注「尚未验证」；目录仅按钮刷新） |
| M09 | 假 CLI 分别返回模型不存在、无权限和强度不支持，暂停派发、有具体原因、不降级模型、不连带认领整个队列 | P0 | 通过（调度 M09a/b：三类分别归类；执行器暂停；另一条目保持 accepted；同问题待处理合并不重复） |
| M10 | 网络、认证、额度与证据不足错误分别按原策略处理，不把所有 HTTP 错误错误归类为模型问题 | P0 | 通过（规则层 M10：404 归 unknown；auth/network 原分类不回归） |
| M11 | 顶栏待处理计数、条目标记与详情原因一致；关闭抽屉、刷新、切换项目及重启后仍可找到，同一问题不反复弹出 | P0 | 通过（API T5：重启后仍在；调度 M09a：合并去重；UI U16 徽标/卡片/面板区） |
| M12 | 修复后仅保存/验证不自动恢复；按原配置恢复保持快照，新配置重试建立关联记录并保留旧模型信息 | P0 | 通过（调度 M12/M13：保存不启动；supersedes 链；原 run 保留；上报后待处理关闭） |
| M13 | 恢复操作重复点击、并发请求或过期 requestId/配置版本只产生至多一个合法执行 | P0 | 通过（调度 M12/M13：重复点击拒绝；过期 runId 拒绝并提示刷新） |
| M14 | 旧 CLI/工具仍活跃、收尾未确认、owner/工作区不符时禁用恢复；启动前配置失败不 claim，运行中失败不回退业务状态 | P0 | 通过（调度 M14：不创建运行不认领；M09a：条目保持 in-progress 不回退；收尾互斥沿用既有机制） |
| M15 | 历史缺模型字段显示未记录；用户补全后创建明确恢复记录，不把当前默认值填进旧历史 | P1 | 通过（恢复无快照记录被拒并指引「以新配置重试」建立明确选择；UI 显示「历史记录未记录」） |
| M16 | 保存新设置保留共用计数器、依赖和 Zcode 数据；不会修改本机配置、权限规则或暴露密钥 | P0 | 通过（API T1/T2：counters/defaults/dependsOn 保留；敏感字段 400；实现只读本机配置不改写） |
| M17 | 运行记录区分请求模型与运行时确认值，缺确认值如实展示，发现不一致时暂停后续派发 | P1 | 通过（调度 M17：确认值单独保存；不一致登记待处理+暂停；UI「按记录配置请求，实际模型未返回」） |
| M18 | 隔离项目中验证账户实际支持的配置：显式选择、单项覆盖、配置改变后原会话续跑，保存实际 CLI/模型参数及输出证据 | P0 | 未执行（真实验证按约定仅由用户明确点击触发，本次不代消耗模型请求；假 CLI 已覆盖参数链路，待人工在隔离项目点「验证所选模型」补证据） |

证据要求（原定）：配置行为采用临时配置目录与可控能力夹具；参数、失败和并发恢复采用假 CLI/服务集成；交互做浏览器验证。M18 使用明确触发的真实模型请求，未执行或账户缺少相应能力时如实记录，不以假 CLI 结果代替。

实际证据：临时 CODEX_HOME/项目 .codex 夹具与假 CLI（`fake-codex.mjs` 增 `debug models` 目录与三类模型失败模式）全部落地；浏览器交互未逐点手动验证，以静态契约测试（U13–U19）+ 既有 UI 测试回归覆盖，剩余人工核对项见 test-report。

