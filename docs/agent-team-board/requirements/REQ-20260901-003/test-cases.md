# 测试用例 — REQ-20260901-003 钩子硬约束插件代码改动：必须先登记并认领条目

> TDD：G1–G6 由 `scripts/tests/code-guard.test.mjs` 覆盖（子进程实测钩子，不经过会话内钩子）。

| # | 用例 | 优先级 | 结果 |
| -- | ---- | ------ | ---- |
| G1 | 无锁：Edit/Write 指向插件源码被拦，提示含流程指引 | P0 | ✓ |
| G2 | 无锁：Bash sed/重定向改插件源码被拦；node 跑测试等只读放行 | P0 | ✓ |
| G3 | 有效认领锁存在：同类 Write/Bash 改写放行 | P0 | ✓ |
| G4 | 豁免与原规则：看板 markdown 直改放行；status.json 直写仍拦 | P0 | ✓ |
| G5 | 只读不误报：cat status.json、只读提及看板目录放行（BUG-20260901-002 修复面） | P0 | ✓ |
| G6 | 项目无看板（无锁）：改插件源码被拦 | P0 | ✓ |

## 执行记录（2026-09-02）

- `node scripts/tests/code-guard.test.mjs` G1–G6 全绿（先红后绿）。首轮 G1/G2"失败"实为测试场景错误——cwd 选在
  插件根时恰有本条目的有效认领锁，守卫放行是正确行为；改用无看板临时目录作无锁场景后通过。
- 九套既有测试回归全绿。
- 实施中发现并修复两个次生误报面（守卫自身被自己的文档转述触发）：
  1. 提及≠目标——status.json 的 Bash 拦截要求**路径形态**（token 带 `/` 前缀），纯文字提及不拦；
  2. 该修正使「写关于守卫规则的文档/测试」不再被守卫阻塞（实测提及式文本 exit 0）。
- 实施细节：插件根以脚本自身 realpath 解析（兼容软链/缓存两种安装形态，当前缓存→软链→源码仓库即时生效）；
  保护=根级文件+scripts/commands/skills/hooks/.zcode-plugin/.codex-plugin/assets，豁免 docs/；放行=当前项目
  看板 .locks/ 存在 24h 内未过期锁；Bash 判定=改写 intent（sed/tee/cp/mv/rm/>/>>/chmod/perl -pi/python -c）
  + 插件源码目标（插件根路径或 agent-team-board/<受保护目录>，避免误拦其他项目同名相对路径）双条件。
- 开放点取默认：任一有效锁放行全区；锁保留至驳回/过期；BUG-20260901-002 一并修复（其 README 已备注）。
