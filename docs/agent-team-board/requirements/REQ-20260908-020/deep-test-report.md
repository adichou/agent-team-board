# REQ-20260908-020 深度测试报告

- 日期：2026-09-08；执行者：Codex。
- 结论：**验收不通过，暂不建议确认完成。** 原有 30 项全部打勾不足以证明验收完成。
- 环境：macOS，本地 Node.js v17.8.0；当前目录没有 Git 元数据，无法提供提交哈希。
- 本次只新增测试证据与缺陷文档，未修改业务源码、未变更需求状态。

## 实测结果

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 全量既有回归 | 88 个测试文件通过，0 失败 | [日志](evidence/deep-full-test.log) |
| 新增业务边界与渲染函数验证 | 12 例，5 通过、7 失败 | [脚本](evidence/deep-probes.mjs)、[日志](evidence/deep-probes.log) |
| 隔离 HTTP 集成验证 | 5 组通过：设置保存读取，以及完善/开发 × zcode/codex 四路启动终止 | [脚本](evidence/deep-http.mjs)、[日志](evidence/deep-http.log) |

这些数字是用例/测试文件数量，**不是代码覆盖率**。原报告的 85% 本次未能用覆盖率采集证明，不沿用为本次测量值。

## 确认的问题

| 优先级建议 | 用例 | 缺陷 | 实际结果与影响 |
| --- | --- | --- | --- |
| P1 | D01、D03 | [BUG-20260908-010](../../bugs/BUG-20260908-010/README.md) | 最后一项执行期间新接受的单在 check 中不被吸收，返回 stop；同轮已完善单驳回再接受后仍被历史 finalByItem 排除。任务漏处理。 |
| P1 | D02 | [BUG-20260908-011](../../bugs/BUG-20260908-011/README.md) | 创建时冻结 README 指纹；领取前人工补充说明后条目被 skipped，未按领取时基线处理。 |
| P1 | D04 | [BUG-20260908-012](../../bugs/BUG-20260908-012/README.md) | 接受新单后重复创建，出现两个未结束完善任务；不符合用例27承诺的单任务及幂等口径。旧队列能力与本需求约定存在冲突。 |
| P2 | D05 | [BUG-20260908-013](../../bugs/BUG-20260908-013/README.md) | codex 批次领取后 run.mode=zcode，提示词也沿用 zcode-refine 的执行者名。 |
| P1 | D06 | [BUG-20260908-014](../../bugs/BUG-20260908-014/README.md) | 终止后接口仍返回 aborted batch，真实面板渲染不再提供启动按钮，不能在面板重新启动。 |
| P1 | D12 | [BUG-20260908-015](../../bugs/BUG-20260908-015/README.md) | 终止面板仍有暂停按钮；pause(true) 把 finished 改为 paused，终态重新进入未结束队列。 |

所有 Bug 通过当前 CLI 独立登记为 submitted，并在说明中关联本需求。当前源码 CLI 已废止 --req 创建参数，未使用旧插件文档中的归属目录机制。

## 已通过的深入检查

- D07：终止后的旧运行迟到 done 被拒绝，新任务的完善中状态不被污染。
- D08：暂停不妨碍在途 fail 收尾；暂停时 next 返回 paused；恢复可继续领取。
- D09：未改文档的 done 被拒绝，保留完善中状态，随后 fail 正常释放。
- D10：任务类型 × Agent 四路配置独立持久化；非法补丁不产生部分保存。
- D11：两个项目中的完善状态互不污染。
- HTTP：四路模型和档位均实际进入创建响应的提示词；完善创建不生成后台运行；领取/终止后详情接口分别呈现 refining/unrefined；终止返回数据与渲染失败用例输入一致。
- 既有回归包含 batch/refine CLI、claim/report、互斥锁、回执大小、动态 planned 队列、服务接口与 UI 静态契约，详见完整日志。

## 验收记录修正

原 test-cases 中第 4 项（实时候选）、第 15 项（Agent 不混用）、第 20/21 项（终止与重启）、第 27 项（单任务与幂等）不能继续视为完整通过。第 9 项仅证明重置索引，未证明重接受单在同轮被处理。

第 18 项仅证实模型配置保存与提示词注入，未证实真实子代理实际采用指定模型/智能档位。design.md 已明确实际传递机制尚待接通。

第 29 项服务重启恢复未在本次新增夹具中做进程重启验证；代码中后台 recover 对 zcode 预留明确交人工处理，与“自动 interrupted 并重置徽标”的原用例描述不一致，原勾选不能作为此结论的证据。

## 测试边界

- UI 深测执行了真实 renderRefinePanel 函数并以真实账本/HTTP 数据确认输入；未做真实浏览器点击、视觉布局、剪贴板及轮询端到端验证。
- 未启动真实 Codex/Zcode 子代理，未验证模型参数被宿主采用。
- 幂等检查为顺序重复请求；未做多进程同时写入 refine/states.json 的压力测试，不能据此保证并发写安全。
- HTTP 夹具首次使用 fetch 与 Node 17 环境不兼容，已改用 node:http，最终完整重跑通过；这是测试夹具问题，不登记产品 Bug。
- 补充测试均使用临时项目；服务退出后清理临时数据。证据脚本保存在需求文档目录，未加入源码测试聚合入口。

## 复测入口

```sh
npm test
node docs/agent-team-board/requirements/REQ-20260908-020/evidence/deep-probes.mjs
node docs/agent-team-board/requirements/REQ-20260908-020/evidence/deep-http.mjs
```

deep-probes.mjs 当前预期以非零退出并报告上述 7 个失败；修复后应以 12/12 通过为最低复测条件，并补齐浏览器与真实子代理验证。
