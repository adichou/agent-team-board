# 测试报告 — REQ-20260914-001 自动提交不完整时挂起条目并暂停队列，人工确认后恢复开发

- 时间：2026-09-14T09:43:11.683Z（本地 17:43）
- 执行者：zcode-batch-048-19（运行 run-20260914-237）
- 测试框架：自研 node 脚本（scripts/tests/*.test.mjs，npm test = run-all.mjs 顺序聚合）
- 覆盖率：未统计（无 instrument 方案；以 21 条验收用例矩阵覆盖口径代替）

## 总结

开发/分析两阶段人工确认闭环全量实施：confirm 账本 + 挂起守卫（batch next / claim / createBatch / refine next）+ 指纹与版本绑定核验 + 授权补交 + 答案回传续跑 + 队列恢复编排 + 任务页卡片与统一确认面板 + i18n。test-cases.md 的 C01–C21 全部通过；新增 3 个测试文件共 23 例全过，`npm test` 232 个测试文件 0 失败，11 个存量测试文件按新队列语义同步更新预期/桩。

## 新增测试

- `scripts/tests/confirm-block-20260914-001.test.mjs`（12 例）：C01 挂起声明与队列暂停、C02 分组失败保留已成功 hash、C03/C04 统一守卫（batch next / claim / 新批次 / 新进程）防绕过、C05 确认→授权补交→测试复验→恢复队列（后续恰派发一次）、C06 内容已变与测试失败保持挂起、C07 幂等（不重复提交/派发）、C08 终端补交识别（不凭任意带单号 commit 直接通过）、C09 纯文档/无改动不误挂、C10 保持挂起与显式取消、C11 清单/详情计数一致、C12 确认不自动 done、C13 核验补交不死锁、C14 历史部分提交账本恢复视图。
- `scripts/tests/confirm-analyze-20260914-001.test.mjs`（7 例）：C15 分析挂起（done 被拒、后续领取被阻）、C16 必答校验与推荐选项不自动作答、C17 草稿/保持挂起/新进程读回（持久化）、C18 答案回传续跑（队首）与收尾闭环后才到下一条、C19 问题清单更新/文档被改 → 过期确认拒绝且轮次留痕可追溯、C20 恢复失败重试保留答案、重复确认幂等、不自动接受/开发/验收、C21 共享清单入口分形态呈现。
- `scripts/tests/confirm-serve-20260914-001.test.mjs`（4 例）：S1 开发侧 HTTP 闭环（清单/详情/单文件差异/保持挂起/重新核验/确认并继续 + 队列恢复编排 + 看板徽标）、S2 分析侧 HTTP 闭环（atb refine hold → answer 草稿 → continue 版本绑定 → 续跑编排 + continuation 回传）、G1 state-guard 拦截（Agent 执行 confirm 人工闭环与 curl 写接口一律 exit 2）、G2 只读接口与 worker 声明通道放行。

## 行为演进与存量用例更新（需求重定义队列语义所致，非回归破坏）

- 自动提交不完整（归属不明/暂扣/失败/无法确认完整）→ 挂起当前条目并持久化暂停队列，`nextAction=stop·paused` 且 notice 指向「待人工确认」入口：`dev-flow-20260911-009` D8、`bug-revert-held-notice-20260914-014` C1 预期同步更新（两测试原意——010 标识清零、暂扣账本机制保持工作——断言全部保留并通过）。
- 新增任务页「待人工确认」置顶卡片、统一侧拉确认面板、Esc 链新层与 i18n 词条：`batch-ui` / `refine-ui` / `bug-refine-done-notice-dup-20260914-001`（vm 桩补 `confirmQueueBannerHtml`）、`detail-close-btn`（Escape 链字面窗口随新层放宽）、`shortcuts-20260910-007` / `impl-entry-ui` / `new-shot-preview-20260910-017`（`#confirmPanel` 初始隐藏态入桩）、`i18n-dict` / `i18n-coverage`（新增词条全部登记，静态/动态词典规则校验通过）。

## 明细

- `npm test`：共 232 个测试文件，失败 0（实施前 229 → 净增 3 个 confirm 测试文件）。
- 真实项目只读冒烟：`atb confirm list` 正确呈现 5 条「历史账本恢复」卡片（C14：仅仍脏路径呈现，已补交不误报）；当前无活动挂起记录。
- 未引入第三方依赖（自研理由与接口/迁移细节见 design.md「实施记录」）；未创建 licenses.md。
- 本单自动提交预期：预留前工作区已存在其他会话遗留脏改动（server.mjs / i18n.js / style.css 等）且本单亦修改了这些文件——按本需求口径将进入「待人工确认提交」挂起（这正是本需求定义的场景），人工核对差异确认归属后由授权补交闭环收尾。

## 人工验证建议（浏览器）

按 README「界面展示」口径验收：任务页顶部阻塞卡片（展开文件表/差异/重新核验/保持挂起/确认并继续，核验中按钮禁用）、分析面板（选项/自由文本/必答就地校验/保存草稿/确认并继续）、队列区「已暂停 · 阻塞于 … · 后续 N 单等待」横幅、看板行「⚠ 待确认提交 / 待确认分析」徽标、页面刷新后卡片与暂停状态恢复（可交互演示对照条目目录 ui-demo.html）。
