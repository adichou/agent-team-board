# M02：格式化字节数（轻量）

实现 `src/m02-byte-size.mjs` 的 `formatByteSize(bytes)`。输入必须是非负安全整数。以 1024 为进位，支持 `B`、`KiB`、`MiB`、`GiB`、`TiB`、`PiB`；选择不超过输入的最大单位，单位值保留最多一位小数并进行常规四舍五入，整数不显示 `.0`。非法输入抛 `TypeError`。只修改 `src/m02-byte-size.mjs`；运行 `node checks/check.mjs M02`；通过后写不超过 2 KiB 的 `receipts/M02.json`。
