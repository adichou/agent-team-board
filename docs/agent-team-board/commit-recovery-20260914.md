# 2026-09-14 未完整提交条目排查与补交

## 范围与结论

用户授权：排查类似 BUG-20260914-008 的漏提交条目，并按依赖顺序一起补交。

- 排查起点：`3b1c284`，分支 `dev`。
- 扫描 `dispatch/runs/*/auto-commit.json`：11 个不同条目含 `pendingManual`，其关联实现/测试在开始排查时仍留在工作区。
- 进一步对照 `build.js` 差异、条目文档与原提交文件清单，发现 3 个更早的部分提交条目：BUG-20260913-004、BUG-20260913-005、REQ-20260913-004。
- 合计 14 个历史未完整提交条目；其中 BUG-20260914-010 的实现正在由另一个会话按 BUG-20260914-014 回退，本次保留回退结果，不重新引入。
- 其余 13 个条目已通过 11 个补交提交纳入 Git。最早 3 个前置条目共同补齐 `build.js`，其后的 10 个条目分别提交。

## 提交顺序与归属

| 条目 | 原因 | 补交 commit |
| --- | --- | --- |
| BUG-20260913-004 | 旧规则未将已脏 build.js 纳入提交；原提交仅包含其他部分 | `26fc9de6` |
| BUG-20260913-005 | 旧规则未将已脏 build.js 纳入提交；原提交仅包含其他部分 | `26fc9de6` |
| REQ-20260913-004 | 旧规则未将已脏 build.js 纳入提交；原提交仅包含其他部分 | `26fc9de6` |
| REQ-20260913-006 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `6aee3737` |
| BUG-20260914-002 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `baede007` |
| BUG-20260914-003 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `06c5c349` |
| BUG-20260914-004 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `7dbcaa56` |
| BUG-20260914-005 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `329e2231` |
| BUG-20260914-006 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `349ee90d` |
| BUG-20260914-008 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `081925d3` |
| BUG-20260914-009 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `1ea53682` |
| BUG-20260914-011 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `d2f32d62` |
| BUG-20260914-012 | 自动提交账本记为 pendingManual，实现/测试暂扣 | `a9fd6ea6` |
| BUG-20260914-010 | 账本记录暂扣；随后人工要求由 BUG-20260914-014 回退 | 不补交已撤下方案 |

前置三个条目的原提交分别为 `69db12b`、`6729acb`、`ea350cc`，均遗漏 `build.js`。其样式、翻译、后端或测试已经入库，补齐该文件后对应前置测试才能共同通过。多个单号保留在共同提交主题中，供看板提交关联检索。

008 在 004 之后提交：004 引入占用状态分支，008 再精简其中的无候选提示。005/006 的早期测试按当时 fetch 行为恢复；011 提交中再同步更新为 fetch + push 的预期，防止前面的提交依赖后面的实现。

## 验证与现场保护

- 从原 HEAD 导出普通临时目录验证各阶段，没有创建 worktree 或额外分支。
- 每阶段运行前置构建界面、服务端、数据层测试及当时已纳入的相关条目测试，阶段测试均通过（每阶段 7～16 个测试文件）。
- 提交通过暂存经核对的阶段内容完成；实际工作区源码与回退 010 后的原内容逐字节一致，没有用中间版本覆盖工作区。
- 本次只补交相关实现、测试及相关条目已有的状态文件变动；没有直接编辑 status.json，也未改变验收状态。
- 保留其他未关联条目的文档、状态修改以及另一会话的 BUG-20260914-014 测试；不混入本次提交。
- 自动提交历史账本保持原样，真实记录当时的暂扣；本文件与新 Git 历史记录补交结果。
- 仅本地提交，未推送远端。

## 后续需求

[REQ-20260914-001](requirements/REQ-20260914-001/README.md)：自动提交不完整时挂起条目并暂停队列，人工确认后恢复开发。需求状态 submitted，本次完成需求登记，不宣称该流程已经实现。

## 最终全量验证

- 在补交后的最终工作区运行 `npm test`：229 个测试文件，失败 0（包括另一会话新增的回退验证测试）。
- 核验补交后的 Git 内容：与逐阶段验证的最终源码快照逐字节一致。
- 核验 `itemCommitStatusIndex`：13 个条目均关联到实际补交 commit。
- `git diff --check` 通过；无本次相关源码/测试遗留，其他会话的未提交文件保留。
